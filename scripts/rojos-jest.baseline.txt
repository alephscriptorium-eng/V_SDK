# =============================================================================
# CONJUNTO DECLARADO DE ROJOS DE JEST — por NOMBRE, no por cardinal · WP-V90
# =============================================================================
#
# Este fichero es el oráculo del gate. Se compara así:
#
#   node scripts/rojos-jest.mjs --gate
#
# El instrumento corre jest él mismo, a un fichero con nonce fuera del árbol.
# Se hace así a propósito: mientras el JSON lo escribiera otro y el gate se
# limitara a leerlo de una ruta convenida, una corrida que revienta sin
# escribir dejaba que el JSON de la vez ANTERIOR pasara por bueno.
#
# Sale 0 si el conjunto obtenido es IDÉNTICO al declarado. Sale 1 ante
# CUALQUIER diferencia, en cualquier dirección: un rojo nuevo, un rojo que
# desaparece, un test renombrado, un test saltado, una suite que deja de
# compilar. Y sale 2 si no hubo medida válida (jest no ejecutó ni un test, o
# el informe trae cobertura, o es rancio) — porque «no medí» no es «verde».
#
# La comparación es de MULTICONJUNTOS: dos rojos con el mismo nombre completo
# no colapsan en uno. Con `Set` —como estuvo escrito hasta la devolución de
# este WP— un rojo NUEVO pasaba invisible con sólo llamarse igual que uno ya
# declarado, y en ese vector el cardinal viejo era MÁS FUERTE que este fichero.
#
# ------------------------------------------------------------------------
# LO QUE ESTE FICHERO **NO** GARANTIZA — léase antes de confiar en él
#
# Cualquiera puede legalizar un rojo nuevo AÑADIENDO SU LÍNEA AQUÍ. Una línea.
# Y nada obliga a arreglarlo nunca. Es diseño —la firma queda en el diff y
# alguien tiene que verla pasar en una revisión—, pero conviene decirlo entero:
#
#   · esto es un .txt SIN checksum;
#   · ninguna línea dice quién la metió, cuándo, ni por qué;
#   · CI no lo vigila (.github/workflows/ci.yml:70 lleva continue-on-error).
#
# O sea: el gate convierte «el mundo empeoró en silencio» en «alguien firmó
# que el mundo empeoró». Eso es lo que se pedía; no es una garantía de que el
# mundo no empeore.
# ------------------------------------------------------------------------
#
# MEDIDO por WP-V90 el 2026-08-01: diez corridas consecutivas del comando
# canónico, con los workers en PARALELO (los de jest por defecto, 11 sobre 12
# CPUs), ejecutando 375 tests cada una, dieron el mismo conjunto las diez veces.
#
# RE-MEDIDO por WP-V48 el 2026-08-01, tras cerrar los cinco: tres corridas del
# mismo comando, también en paralelo, 375 tests cada una — el mismo cardinal que
# midió V90, porque V48 no añadió ni quitó ni un `it`—, mismo conjunto las tres
# veces y `jest exit=0` en las tres. Antes de V48 ese exit era 1.
#
# Lo que NO está aquí, y por qué: los umbrales de cobertura. Jest sale con
# código 1 por ellos, pero en su JSON no queda ni rastro del motivo, así que
# no tienen nombre que declarar. Por eso el comando canónico lleva
# `--coverage=false` y la cobertura se comprueba aparte.
#
# Estado medido por V90:  statements 24,91 % · branches 24,90 % · lines 25,33 % · functions 20,49 %
# Estado medido por V48:  statements 26,10 % · branches 25,13 % · lines 26,55 % · functions 21,51 %
# Umbrales:                          85 %                75 %             85 %               80 %
#
# La subida NO es mérito de cobertura nueva: es que los cinco tests que morían
# en el constructor de `TerminalManager` ahora recorren de verdad
# TerminalManager, ProcessManager, WebViewManager y CommandPaletteManager. Un
# rojo también tapa las líneas que no llega a ejecutar. Sigue siendo un rojo
# abierto y documentado desde WP-V13; NO era de V90 ni de V48 cerrarlo, y este
# fichero no lo tapa: lo señala.
#
# Las líneas que empiezan por «#» y las vacías se ignoran.
#
# -----------------------------------------------------------------------------
# LOS CINCO ROJOS DETERMINISTAS · CERRADOS POR WP-V48 (2026-08-01)
#
# Aquí se declaraban cinco FALLA, todos de
# `tests/integration/managerFactory.test.ts`:
#
#     … Manager Creation should create process manager
#     … Manager Creation should create webview manager
#     … Performance should handle concurrent manager creation
#     … Standard Managers Creation should create all standard managers
#     … Standard Managers Creation should have proper dependency chain in standard managers
#
# Los cinco compartían UNA sola causa, y la contrarrevisión la había dejado
# escrita aquí. V48 la verificó abriendo los ficheros y corriendo la suite: los
# cinco lanzaban exactamente
#
#     TypeError: vscode.window.onDidCloseTerminal is not a function
#         at new TerminalManager (src/terminalManager.ts:24)
#         at new ProcessManager  (src/processManager.ts:33 → :38)
#         at ManagerFactory.createManager (src/core/managerFactory.ts:71, y :75 vía webViewManager.ts:42/:51)
#
# El origen NO era el mock compartido: el fichero declaraba su PROPIO mock
# inline `{virtual:true}` en :9-53, con un `window` de cuatro claves y ninguna
# `onDidCloseTerminal`. El compartido `tests/mocks/vscode.mock.js` SÍ la expone
# (:83) desde dfddc87 (WP-V66) — pero no alcanzaba a esta suite.
#
# CÓMO SE CERRÓ, y por qué importa para el que lea esto dentro de tres olas:
# no añadiendo la clave que faltaba. Ése habría sido el arreglo del SÍNTOMA: el
# mecanismo del fallo es que este fichero mantenía una COPIA del mock que el
# resto de la suite ya tenía completa, y las copias se quedan atrás — de hecho
# ya se había quedado atrás una vez, cuando V66 amplió el compartido. El mock
# inline pasa a DERIVARSE del compartido (`jest.requireActual`) y conserva sólo
# el único trozo que aportaba algo propio: la `getConfiguration` consciente de
# la sección `aleph0.logging`, sin la cual `LoggingManager` recibiría
# `enabledCategories='test-value'`. `tests/mocks/vscode.mock.js` NO se tocó, así
# que ningún otro fichero de la suite cambia de comportamiento.
#
# Ninguna aserción se ablandó y ningún test se saltó: el fichero sigue teniendo
# 19 `it`, los mismos 19 nombres, y ahora los 19 en verde. MEDIDO con
# `--gate`: al aplicar el cambio, el conjunto obtenido difería del declarado en
# exactamente cinco líneas, las cinco en dirección «-», y ninguna «+».
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# OMISIÓN DECLARADA (una)
#
# Un `skip` no es un verde: es un rojo escondido, salvo que esté firmado. Éste
# lo está —es el «skip-honesto» de WP-V28— y por eso figura aquí por su nombre
# completo. Que aparezca en el conjunto es deliberado: si mañana alguien
# saltara un test para quitarlo de la lista de rojos, saldría en el diff igual
# que sale éste.
# -----------------------------------------------------------------------------

OMITE tests/unit/mcp/clienteMcp.test.ts :: [pending] WP-V28 · contra runtime real (skip-honesto) skip-honesto: conectar/listar/leer contra mcp-launcher y linea-editor VIVOS de z-sdk — exige runtime real de la Ciudad arrancado (no disponible en este arnés unit); no se simula como verde
