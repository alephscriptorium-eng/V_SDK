# BACKLOG — carril V · Aleph-0 (ℵ₀)

| dato | valor |
| ---- | ----- |
| Mundo | `C:\S_LAB\v-sdk` — el **Zigurat**: el IDE desde el que se opera la Ciudad |
| Serie | `WP-Vnn` · método `swarm-orquestacion` · estados ⬜ pendiente · 🔶 en curso (exige worker+claim) · ✅ aceptado |
| Fuente normativa | **INFORME-R4** + consenso H-01 (env obligatorio · 4a · C1 preferente · CA en ticks nuevos) |
| Edición | **F2-unificada** (2026-07-26) — refactor del Anfitrión sobre F2 de V + revisión Temis; un solo plan Scriptorium, seis carpetas |
| Doctrina | INÉDITO (primera versión; nadie migra porque nadie usó) · CERCO v2 (§10.8 local-first) · estructura antes que interfaz · centro vacío |

---

## Visión del mundo acabado

> El editor es del usuario del Scriptorium: **el centro se mantiene vacío**.
> Todo lo mío vive en la periferia. Acabado significa: **desde este IDE se
> entra a la Ciudad, se la ve entera sin que la vista mienta, se la manda,
> y se edita lo que el contrato declare editable — instalable por un
> desconocido en una máquina limpia, sin heredar un solo `✅`.**

Invariantes de todos los lanes: **no mentir** (⏳ visible, nada inferido) ·
**ámbitos, no cadenas de mando** · **toda conexión con fila declarada**
(catálogo, segunda puerta documentada, o «sin superficie» con motivo) ·
**cerco v2** (local-first; peers del contrato sí, anclas vivas no).

**Dependencias externas de este plan** (un solo Scriptorium): la puerta del
grafo y sus 2 modalidades = **Z-D1/U233-235** · estructura playground =
**HUB/G52** · fichero env = **O-c** (O posee schema con Z; V posee editor) ·
orquestador de arranque que el mando de ciudad consume = **U234** · matriz
de canal/puertas = **U236**.

---

# F2 · Lanes hacia el mundo acabado

## LANE A · ESTRUCTURA (antes que interfaz) — **P0**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V21** `P0` ✅ | **Gate de consumo de la estructura del Zigurat — VEREDICTO: ABIERTO** (`plan/GATE-V21-consumo-estructura.md`): 13 rutas citables verificadas con ojos propios (greps con exit code · contraste de citas contra el repo G vía `git show` · mapa↔grafo↔V22 fila a fila). El gate tuvo dientes: **H1/H2/O2/O3 enrutados a G** (base de `ZEUS_STARTPACK_ROOT` sin declarar · fallback dev pre-throw en `startpack.mjs:21-25` · censo no machine-readable · ruta relativa cosmética) y **O4 a V19/V20** (4 packs sin entrada en CATALOG_SEED → las vistas pintan ⏳, no fingen). **La UI de V queda desbloqueada.** Aceptado 2026-07-31 (rama `wp/v21-gate-consumo`) | estructura citada por ruta ✓ · frontera estructura/lienzo ✓ (cero fugas) · cero VS Code hasta el gate ✓ (diff = 1 doc) |
| **WP-V22** `P0` ✅ | **Mapa barrio → superficie**: `plan/MAPA-SUPERFICIES.md` — las 14 entradas de `CATALOG_SEED` mapeadas (4 «no va (aún)» por `workspace: null`, destino V19 contra V20; filas solar condicionadas a V57/V20 y así declaradas); 43/50 piezas del workspace fuera del catálogo (denominador cerrado por U179: 51 = 50 workspace + 1 anidada). Aceptado por el orquestador 2026-07-31 (rama `wp/v22-mapa-superficies`) | tabla completa sin `<pendiente>` ✓ · cada «no va» con motivo ✓ · ninguna fila implica jerarquía ✓ (citas muestreadas por el orquestador contra z-sdk) |
| **WP-V27** `P1` ✅ | **Léxico del Zigurat**: `plan/LEXICO-ZIGURAT.md` — 17 términos con fuente ruta:línea (acuñaciones declaradas: pieza·superficie·Zigurat); la «ontología» no existe como doc único — el glosario cita cada fragmento donde vive. **Tabla de 10 colisiones** con greps y conteos: C1 la crítica (UI dice «servidor»×40 donde el plan dice «pieza» — contradice U217 de Z; canónico = pieza → **insumo de V23**) · C3 peercard con 3 grafías · C4 «asiento» polisémico (regla: prohibido sin calificar) · C5 `src/launcher/` alberga catálogo (→V19/V23). Regla al pie: término nuevo sin fila = FAIL. ⏳ «maquinaria» reservada hasta que G la fije (U216). Aceptado 2026-07-31 (rama `wp/v27-lexico`) | glosario citable ✓ · cada término con fuente ✓ · colisiones detectadas exhaustivas ✓ · sin jerarquía ✓ |

## LANE B · ENTRADA E IDENTIDAD — **P0**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V28** `P0` ✅ | **Cliente MCP mínimo**: `src/mcp/` (fetch nativo, JSON-RPC HTTP, resultado honesto tipado; endpoints por config→env central de Z por NOMBRE, cero literales). Contratos citados de z-sdk RO (`editor://info` editor-server.mjs:135 · `launcher://catalog` launcher-server.mjs:42). Verificación contra runtime real = skip-honesto, cierra con V18/Z-D1. Candidato dedup anotado: migrar `src/launcher/`+`src/resources/` a consumir `src/mcp/` (mataría los 8 errores TS preexistentes). Aceptado 2026-07-31 (rama `wp/v28-cliente-mcp`; re-verificado por orquestador: 21+1skip, grep literales=0, deps-diff=0) | conecta ✓ (fixture declarado) · lee ambos resources con forma citada ✓ · falla honesto sin runtime ✓ (⏳ tipado, cero throw) |
| **WP-V18** `P0` **BLOQUEA:** holón-7 | **Entrar al grafo como edificio-1** y marcar mi fila. **Sin dilema** (consenso): la puerta es `rooms`/`socket-server` (`CLIENT_REGISTER`, la card **viaja** — verificado por Z); anónimo base y card opt-in son **las dos modalidades del mismo contrato** (§2.a) y el CA de Z-D1 exige ambas. El torno WebRTC es otra capa con dueño y WP (Z·U186); no condiciona esta entrada | fila V marcada · modalidad usada declarada en la marca · evidencia de facto contra runtime (dep **Z-D1**) · cero escritura fuera de mi fila |
| **WP-V29** `P1` | **Peercard opt-in**: emitir/portar card, seat vía API del protocol (cero cripto propia) | join→card→resources demostrado · card expirada ⇒ re-join · sin card, anónimo funcional |
| **WP-V30** `P1` | **Anónimo honesto**: la UI declara modalidad y capacidades ausentes | modalidad visible siempre · denegación con porqué · ausencia de card ≠ error |

## LANE C · CONFIGURACIÓN — el editor de la demo — **P0**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V26** `P0` | **Editor del fichero env real** del playground (O-c): **V implementa el editor; el schema/validación son de O+Z**. Única interfaz V↔O, nace ahora | edita el fichero real generado desde `presets-sdk/env` · cero ajustes locales compitiendo · resuelve dentro del cerco · schema consumido, no inventado |
| **WP-V31** `P1` | **Endpoints por variable, nunca por número** | grep de puertos literales fuera de defaults de schema = 0 |
| **WP-V32** `P1` | **Validación honesta del env** | cada clave ✅/⏳/⛔ · ninguna inventada · sin red sigue diciendo verdad |
| **WP-V33** `P2` | **Perfiles de entorno** (local/lab/vps) por contrato, no por path | cambiar perfil no edita rutas a mano · perfil activo visible |

## LANE D · CATÁLOGO Y MANDO DE CIUDAD — **P0/P1**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V20** `P0` **BLOQUEA:** modelo de integración | **Documento de puertas, conjunto con Z** (dep U236): todo lo consumible tiene fila — catálogo, segunda puerta declarada, o «sin superficie» con motivo. Cero clientes a medida furtivos | documento con fila por pieza consumida · firmado por ambos lados (V consume · Z posee canal) · V24 y V28 lo citan |
| **WP-V19** `P1` | Las 4 entradas del catálogo que no lanzan: causa y destino o descarte | cada una con causa · destino o «sin superficie» razonado |
| **WP-V34** `P1` | **Mando de ciudad**: launch/stop/restart de barrios **consumiendo el orquestador de Z (U234)** — el IDE manda, el runtime orquesta; cero spawn a mano en la extensión | arranca y para un barrio real vía contrato · estado real, no optimista · terminal por barrio |
| **WP-V35** `P1` | **Salud de barrios** con ⏳ honesto | salud real · sin runtime, ⏳ y no error fatal |
| **WP-V36** `P1` | **Árbol de ámbitos**: zonas que solapan y enlaces horizontales dibujables | una zona en dos sitios se ve como una · nada sugiere que arriba manda |

## LANE E · OBSERVACIÓN DE CAPAS SUPERIORES — **P1**

*(dominio; el transporte lo observa la Admin UI de O — sin solape, cerrado en R5)*

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V37** `P1` | `console-monitor` → panel 1 | pieza real, no espejo · vacío honesto |
| **WP-V38** `P1` | `firehose-browser` → panel 2, **por cursor** (flujo, no pozo) | navega por cursor · no promete totales incontables |
| **WP-V39** `P1` | `cache-browser` → panel 3 | cacheado Y no-cacheado visibles · cache miss visible |
| **WP-V40** `P2` | Visor de story-boards, personajes refs-only | refs-only · ciclo raw→triaged→canon visible |
| **WP-V41** `P2` | **Elenco vivo** con `ICompany` separada por contrato | dos modelos documentados · reparto real |

## LANE F · VOLUMES DESDE EL IDE — **P1** *(consenso H-01 aplicado)*

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V42** `P1` | **Lector del adaptador**: namespace lógico, root por **env obligatorio** (◆5 votada; jamás cwd) | pieza citada por nombre lógico · root usado visible · dos cwd, mismo resultado |
| **WP-V43** `P1` | **Manifiesto ≠ estado** como dos hechos; la deriva es dato | ambos a la vez · divergencia visible, no fatal |
| **WP-V44** `P1` · compromiso **T9** | **Evidencia de réplica verificable por tercero** (HUB-084 consume esta evidencia) | verificación desde fuera de A y B · autocertificación rechazada por diseño |
| **WP-V45** `P1` | **Procedencia + hash tras import** legibles | cada pieza dice qué es y de dónde vino · URL externa = sidecar inerte |
| **WP-V46** `P2` | **Curación humana declarada**; el import nunca la pisa | opt-in explícito · import respeta o falla ruidoso |

## LANE G · PRIMERA VERSIÓN INTENCIONAL (deuda → demolición) — **P1**

*(INÉDITO: no hay usuarios que migrar; hay caminos que retirar con acta)*

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V23** `P1` ✅ | **ACEPTADO 2026-08-01** (merge `12de442`, 4 rondas · 3 contrarrevisiones). **26 claves → 18**, un solo namespace, y **ninguna clave vieja sobrevive** (el revisor lo midió con patrón más ancho y reclasificó las 116 residuales **desde cero**). **Siete demoliciones**, no seis: la séptima nace al hallar una clave con el lector **tapiado por los dos extremos** — eligió **demoler y no cablear**, porque no hay un solo cliente de esa integración y cablear habría sido **inventar funcionalidad**. Colisión **C1** saldada (19 descripciones, `servidor\|servicio` = 0). Y **saca a la luz el defecto inverso**: 21 claves leídas de una sección que no las declara, **dos gobernando el arranque** sin que el usuario pueda verlas. **Donde más se aprendió fue en lo que no arregló**: declaró cinco pérdidas silenciosas y había **seis** — la sexta no era silencio sino **sustitución** por un valor plausible que alimenta el contexto de toda la extensión; **retiró su propia bendición** (cumplía el contrato del *método* y lo rompía la *superficie*); y no tocó conducta, arregló **la mentira** — el docstring que decía «no inventa localhost:puerto» **escrito encima de la línea que lo inventa**. Su hallazgo mayor ni era del WP: **la suite no es determinista** (11 corridas `5,5,5,6,7,5,…`) → **V90**. Cierra con `aleph0` por **DV-17**. ✎ **Conflicto de carriles resuelto por el orquestador** (la rama llevaba 4 rondas mientras V66 y V71 aterrizaban en los mismos ficheros): 4 conflictos mecánicos, resueltos conservando **las tres intenciones**, con cero claves viejas resucitadas y cero `console.*` nuevos. ✎ Histórico: **Entregado y devuelto** (2026-07-31). Resiste con nota: **ninguna clave vieja sobrevive** — el revisor reclasificó las 116 residuales **desde cero** con cubos disyuntos y patrón más ancho, cero configuración · acta completa (26 filas = 26 claves de la base) · las 6 demoliciones **verificadas contra la base**, ninguna función rota · las 21 huérfanas son 21 y el renombrado no crea ninguna · léxico con citas exactas y **C1 saldada de verdad** (19 descripciones, `servidor\|servicio` = 0) · números honestos · cero contrabando. Pero dos bloqueantes: **D1** `aleph0.pieza.ollama.baseUrl` es **clave declarada sin efecto** (su lector es código muerto en cadena de 3 saltos) — y el acta demolió 6 claves con ese mismo criterio, **tres de ellas con más lector que ésta**: o son 7 demoliciones, o se cablea · **D2** el **quinto silencio**, que no es silencio sino **sustitución**: `getDefaultSocketUrl()` (`mcpConfigurationManager.ts:214-225`) cae a `ws://localhost:${port}` sin ⏳ ni log, y ese valor alimenta el `socketUrl` de **toda** la extensión — el usuario que pierde el mesh ve su monitor apuntando a su propia máquina. Menores: la CA de V05 se dio por re-verificada con un patrón que **no ve el `localhost` interpolado** que hay (verde vacío) · la fusión omite que se **auto-adopta y auto-escribe** un fichero de la raíz en los ajustes. **Pregunta abierta al custodio** (D5): la cita de DV-16.a es verbatim correcta pero elide «**dentro de WP-V15**», que es la cláusula que decide si ata a V23; y `aleph0` **no tiene fila en el léxico** mientras `Zigurat` sí. | **Primera configuración intencional**: un solo espacio de nombres salido de la ontología; los 3 prefijos actuales se **demuelen**. **Ola 3, despachado 2026-07-31** (`wp/v23-config-intencional`, worktree `wt/v-v23`) — **único escritor de `package.json` esta ola** (cadena crítica §1); consume la colisión **C1** del léxico (*servidor* → *pieza*) · **contrarrevisión obligatoria** (contrato/config) | un namespace · acta de demolición · CA de V05 re-verificada |
| **WP-V24** `P1` | **Demoler el cliente 3010** → conector por catálogo/puerta declarada (dep V20) | cero puertos a mano · endpoint resuelto, no escrito |
| **WP-V25** `P1` ✅ | **ACEPTADO 2026-08-02** (obra `fc74e8e`, suelo `b0c57af`, reporte `d435aee`, cierre `1fafc81`, merge `f3af546`; CI verde en rama; **contrarrevisión adversarial PASS — el 3.º en ~49** + ronda de cierre). **La ficha era EXACTA: 31**, corroborado por tres métodos, uno **dentro de un VS Code real**. **Lo que estaba mal era el método de contar, por los dos lados**: el grep ingenuo da 158 porque la clave está también en `menus`/`keybindings`; y contar sólo `commandTable` da 56 y es **igual de falso**, porque `CommandPaletteManager` registra **16 más desde su constructor** — y `index.ts` afirmaba, cita rancia, que «ningún módulo registra por su cuenta». **Censo**: 99 entradas / 98 ids / 72 registrados / 31 sin handler / 5 sin declarar / 1 duplicado → **91 declarados / 93 registrados / 2 internos: biyección**. **Nueve de los 31 no eran adorno**: destino de un clic o de una llamada ya escrita → **error crudo, no silencio**; entre ellos **cuatro que SÍ tenían handler y morían invocando a un ausente**, dos con atajo. **Tercera clase cerrada** (`logs.showEntry`, ni declarado ni registrado, invocado desde cada entrada del árbol de logs) y un **id mal escrito**. **20 cableados · 11 retirados con acta · CERO handlers vacíos** — un handler vacío es la misma mentira con otra cara. **De facto**: `test:exthost` verde en VS Code **1.131.0** real, corrido también **por el contrarrevisor**. **El PASS se ganó atacando**: re-derivó cada cifra por método propio y **aisló el movimiento del suelo** corriendo la suite sin ese único test — salió el suelo viejo clavado, o sea que **el argumento de ramas era medible**. **Su corrección a la ronda de cierre del orquestador es la buena**: arreglar sólo la fórmula de colisiones habría sido **teatro**, porque la medida venía de un `Map` que **ya deduplica** — **tautológica por construcción**. Arregló las dos, y **su test estrena, por primera vez en la historia del repo, la rama del `warn` de duplicado**: el punto ciego no era sólo del gate, nadie había pasado nunca por ahí. **Declarado a petición del orquestador**: **5 de los 20 (25 %) aterrizan en simulación** — afirman al usuario haber hecho algo que no hicieron — y **`configs.format` es el único efecto destructivo que este WP hace alcanzable** (reescribe el fichero original sin confirmación ni copia, y el árbol escanea `**/package.json`) → material de ficha propia. **Suelo movido dos veces y firmado las dos**; trinquete re-medido por el orquestador tras fusionar V25 y V99: **1816/547/352/1780, exit 0, exacto en el suelo** | cero comandos que prometan lo que no hacen ✓ (biyección, y gate que enrojece en las dos direcciones) |
| **WP-V47** `P2` | **Retirar la marca previa** de las 5 superficies (`ARRAKIS_*`) | quien instala no lee marca ajena en ninguna superficie |
| **WP-V48** `P2` ✅ | 5 jest rojos → **guarda del mando de ciudad** (V34). **✎ REENCUADRE 2026-08-01 (de V90)**: los cinco tienen **UNA SOLA CAUSA** y **no es el mock compartido** — el mismo `TypeError: vscode.window.onDidCloseTerminal is not a function`, con origen en el **mock inline** de `tests/integration/managerFactory.test.ts:9-53`. **✅ ENTREGA 2026-08-01** (rama `wp/v48-cinco-rojos`, base `629d502`, reporte `plan/REPORTES/WP-V48-cinco-rojos.md`). **El reencuadre era exacto**: verificado corriendo el fichero — los cinco, una sola excepción, un solo origen (`src/terminalManager.ts:24`, llamada **en el constructor** de `TerminalManager`), y son exactamente los cinco tests que llegan a construirlo por `ProcessManager` o arrastrado por `WebViewManager`. **No se arregló añadiendo la clave que faltaba**: eso es el síntoma. El mecanismo es que este fichero —**el único de los 10** con `jest.mock('vscode')`— mantenía una **copia** del mock, y las copias se quedan atrás: ya se había quedado atrás una vez, cuando `dfddc87` (V66) amplió el compartido *precisamente porque `TerminalManager` la necesitaba*. Ahora el mock **deriva** del compartido (`jest.requireActual`) y conserva sólo lo único que aportaba de propio: la `getConfiguration` consciente de `aleph0.logging`. **Y eso no era adorno**: medido con sonda, sin ese trozo `LoggingManager` recibiría `enabledCategories='test-value'` → `new Set(…).size = 8`, ocho categorías de una letra — o sea que **borrar el mock entero también ponía los 19 en verde, degradando en silencio el objeto bajo prueba**, y nada en la suite lo habría notado. `tests/mocks/vscode.mock.js` **no se tocó** (radio de explosión nulo por construcción, no por medida) y `src/**` tampoco. **Números**: suite 375 → 375 tests (ni un `it` añadido ni borrado), 5 rojos → **0**, 369 → 374 verdes, suites `1 failed`+9 → **10 passed**, jest exit 1 → **0**; cobertura 24,91/24,90/20,49/25,33 → **26,10/25,13/21,51/26,55**. **Gate**: antes de tocar la línea base, `--gate` dio **5 líneas «−» y cero «+»** — ningún rojo nuevo en los otros 9 ficheros; después, `IDENTICO` exit 0. **Diff de la línea base: 5 entradas retiradas, 0 añadidas.** **No se ablandó nada**, y es comprobable con grep: el diff del test **no contiene ni una línea** con `it(`, `expect(`, `describe(`, `.skip`, `.todo` o `xit(` — los dos hunks caen dentro del bloque del mock. Determinismo: 3 corridas en paralelo, mismo conjunto, 375 tests cada una; **ningún rojo intermitente** en 7 corridas completas. **Dos hallazgos**: (a) `require` (en vez de `requireActual`) dentro de la fábrica **recursa** porque el `moduleNameMapper` resuelve `'vscode'` a ese mismo fichero — `RangeError: Maximum call stack size exceeded`, **suite entera caída, 0 tests ejecutados**; (b) la pista `H-8` de `WP-V23:616` está rancia por dos motivos **verificados con git**: nombra el fichero equivocado, **y** su mitad factual caducó — en `b97151b` el compartido **no** tenía la función, y `dfddc87` **no es ancestro** de `b97151b`. **↩ DEVOLUCIÓN (2ª vuelta, sólo prosa — cero líneas de código): cayeron dos frases mías, y la CA de terminales con ellas.** Escribí que la subida de cobertura era que los cinco «ahora **recorren** el ciclo de vida de terminales». **Falso, medido por fichero**: `src/terminalManager.ts` sale **bit a bit idéntico** (lines 9/97 · functions 1/14 · statements 9/99 · branches 0/27 — **9,09 % y 0 % de ramas** en los dos lados). Los +70 statements están **en otros ficheros**: `commandPaletteManager` **+40**, `managerFactory` +11, `processManager` +6, `mcpConfigurationManager` +6, `webViewManager` +4, `loggingManager` +2, `structuredLog` +1, **`terminalManager` +0**. La causa es que **istanbul incrementa el contador de la sentencia antes de evaluarla**, así que el constructor ya contaba como cubierto en la corrida roja: «un rojo tapa lo que no llega a ejecutarse» es cierto en general y **falso justo donde muere la excepción**. Y la segunda frase: mi tabla describía el recorrido **en aislamiento** presentándolo como el de la corrida medida — con sonda, en la corrida canónica se construye **UN SOLO `TerminalManager`** (en `should create process manager`); los otros cuatro reciben los singletons estáticos, así que la cadena «webview → process → terminal» **se corta en process**. En aislamiento (`-t`) los cuatro sí lo construyen: **los tests no están vacíos, la frase estaba mal**. **Consecuencia asumida: la CA «cubren ciclo de vida de terminales» NO la cierra este WP** — las 29 aserciones ejecutadas son `toBeDefined`/`toHaveProperty('dispose')`/`hasManager`/`toHaveLength(5)`, **ninguna nombra un terminal**, y el callback de `onDidCloseTerminal` **no se invoca jamás**. Lo que sí cierra: la **cadena de construcción** de managers se completa sin lanzar. Fila propuesta para la mitad que falta en **§10 del reporte** (`WP-V48b`), no autoasignada. **Agravante que consta**: la frase falsa la sembré en `scripts/rojos-jest.baseline.txt`, artefacto compartido — un WP que existe para desmontar una nota rancia sembró una **nota nacida falsa** en el sitio de máxima propagación; ya está corregida ahí con el desglose por fichero. **Lección nueva para el método (§11.1)**: *una cifra que sube no dice por qué sube — antes de atribuirla, mírala por fichero*. **Precio ampliado a 6 puntos** (§2.2): mi override redeclara la forma del objeto de config (`{get,update}` vs `{get,update,has,inspect}` — el mismo mecanismo una capa más adentro, inocuo hoy y medido) · `onDidChangeConfiguration` pasa a devolver `undefined` (4 consumidores: 2 lo descartan, **2 lo guardan**) · desaparece `ExtensionContext` (sólo tipo). **Deuda de producto ampliada**: la suscripción-en-constructor se repite en **4 clases**, **2 en la ruta de estos tests** (`LoggingManager:60`, `ConfigurationService:242-244→426`); NO tocada | los 5 en verde ✓ · cubren ciclo de vida de terminales ❌ **no la cierra este WP** (§4.1 y §5.3.bis del reporte) → `WP-V48b` |
| **WP-V91** `P1` ✅ | **ACEPTADO 2026-08-01** (merge en `main`; 1 contrarrevisión con 3 bloqueantes + corrección + verificación del orquestador) (1 contrarrevisión con 3 bloqueantes + esta corrección) · rama `wp/v91-gate-del-instrumento` · reporte `plan/REPORTES/WP-V91-gate-del-instrumento.md`. **Los tres bloqueantes eran la misma cosa: el conjunto de mutantes lo elegía yo, así que la suite cubría las vías que se me habían ocurrido — que es la lección que yo mismo había escrito en la primera entrega.** **(B1)** la dirección **«−» del diff** —un rojo que DESAPARECE— tenía **cero** aserciones: mutándola, «IDÉNTICO», EXIT 0 y los 28 tests en verde; era una de las cuatro garantías que mi propio reporte enumeraba, o sea un gate contra el encogimiento **sin un solo test de encogimiento**. **(B2)** la guarda de paralelismo se esquivaba por **siete formas más** (`--max-workers=1`, `--runInBand=true`, `-i=true`, `--maxWorkers=01/1.0/+1`, `10%`): mi arreglo cerró **2 de 9** y mi comentario afirmaba lo contrario; ahora la guarda **no compara formas, analiza el argumento** (canonicaliza nombre, resuelve valor pegado o suelto, y los porcentajes con `floor(cpus×pct/100)`, fórmula medida en 5 puntos) y caza **11 formas medidas** con 4 controles que no deben caer. **(B3)** el mutante de orden **por comparador de locale** sobrevivía 28/28 porque mi fixture era todo ASCII minúscula, **donde los dos comparadores coinciden** — regla que queda: *una fixture que no distingue dos implementaciones no vigila ninguna*; basta una mayúscula y una eñe. **Menores cerrados**: fuga de **5 directorios temporales por corrida** (`process.exit()` no ejecuta los `finally`; ahora cuelga de `process.on('exit')`) — heredado de V90 pero que yo convertía de raro en «cada `npm test`», y que desmentía mi propio «nada escrito fuera del worktree» · `--gate` **a secas** (baseline por defecto) · **detector de discrepancias** de `--repetir`, que declaré incubrible sin un test que flapee y se cubre con un contador en disco que falla sólo en la primera corrida · veredicto en serie sale 1 · validación de `--repetir N`. **Coste temporal, bien contado**: la suite del gate pasa de **~25 s a ~50 s** y este fichero es el camino crítico — el «16 s frente a ~90 s» de la primera entrega estaba mal dos veces. De **28 a 36 tests**; censo de mutación de **15 a 27, cero supervivientes** (una fila salió «superviviente» por un **no-op mío** al escribir la mutación, corregido y contado). `--gate` sobre la suite completa: `IDENTICO`, exit 0, **411 tests**. Obra previa `b8f341c`. **El instrumento de rojos no tenía gate propio** (declarado por el worker de V90 en §9.1 al cerrar, no hallado por nadie contra él). `scripts/rojos-jest.mjs` vigila la suite entera y **sus cuatro clases (`FALLA`/`OMITE`/`SUITE`/`SINNOMBRE`) y sus tres guardas (multiplicidad, ejecución efectiva, frescura) no tenían un solo test que las ejercitara**. Era un gate sin gate — y las tres guardas existen **precisamente porque las tres fallaron** en contrarrevisión. **Entregado**: `scripts/tests/rojos-jest.test.ts`, **28 tests en 16 s**, corre con `npm test`. Cada clase y cada guarda con su caso rojo y anclada a un **mutante**: se desactiva el trozo que el test dice vigilar y se EXIGE que las aserciones dejen de cumplirse. **Censo de mutación sobre el instrumento en el árbol: 15 mutaciones, CERO supervivientes**, y cada una enrojece los tests que la nombran. Aislado del producto (informes sintéticos + proyectos jest MÍNIMOS en `os.tmpdir()`; **no corre la suite del producto ni una vez**), con las cuatro clases, la multiplicidad y el vector real de la guarda 2 medidos **también contra jest 29 real**. **Dos defectos reales hallados atacando y arreglados**: `--edad-max` sin valor o con «900s» daba **NaN**, y `NaN > maxSeg` es false → la guarda de **frescura se apagaba sin aviso** (MEDIDO: informe de hace una hora, «IDENTICO», EXIT 0 en las tres formas); y `--maxWorkers 1` / `-w 1` **en dos argumentos** esquivaban la guarda de **paralelismo** (MEDIDO con 6 suites que anotan su PID: las cuatro formas dejan jest en UN proceso). **Y cuatro mutantes sobrevivieron a la primera versión de la suite** —24/24 en verde con el instrumento roto— atacándola por vías que ella no contemplaba: **orden canónico invertido** (la promesa de «byte a byte igual» era la menos vigilada), `--gate` sin imponer `--coverage=false` (sin eso `SINNOMBRE` no salta), normalización de ruta de un informe nacido en CI, e informe fechado en el **futuro**; los cuatro tienen test. **El baseline NO se mueve**: `--gate` sobre la suite completa da `IDENTICO`, exit 0, **403 tests** (375 + 28). ✎ La frase que V90 dejó escrita queda sostenida **como test con nombre**, no como nota al pie: **el gate convierte «empeoró en silencio» en «alguien firmó que empeoró»; NO garantiza que no empeore** — el baseline es un `.txt` sin checksum, sin dueño por línea y sin gate en CI, así que un rojo nuevo se legaliza añadiendo una línea | cada clase y cada guarda con su caso rojo ✓ (15/15, mutación con 0 supervivientes) · mutar el instrumento pone rojo su propia suite ✓ (medido en el árbol) | P1 |
| **WP-V93** **`P0`** ✅ | **ACEPTADO 2026-08-01** (merge `6e0347a`; 1 contrarrevisión con 4 bloqueantes + corrección + verificación del orquestador). **CI no vigilaba NADA** (hallado por V91 al cerrar, **confirmado y ampliado por su contrarrevisión** con tres datos medidos): **(a)** `.github/workflows/ci.yml:70` lleva `continue-on-error: true` sobre el paso que corre la suite — declarado y documentado, pero declarar no es proteger; **(b)** **ningún paso de CI ejecuta el gate** (`grep rojos-jest|--gate .github/` = **0**), así que todo el aparato de V90+V91 —el instrumento de rojos por nombre, sus cuatro clases, sus tres guardas y sus 36 tests— **tiene fuerza sólo en local**; **(c)** ese paso está **permanentemente en rojo**, con lo que su aportación real es cero. **El terreno cambió al cerrar la ola 5 y eso reencuadra el WP**: V48 llevó los 5 rojos a 0, así que **la suite ya pasa** (411 tests · 410 pass · **0 fail**, medido por el orquestador en `main`). Lo único que mantiene el paso rojo son los **umbrales de cobertura** (`jest.config.js:23-30`: 75/80/85/85) frente a un **~25 % real** — y `collectCoverage` está activo por defecto (`:14`), así que `npm test` los evalúa siempre. **Por eso el WP NO es «quitar `continue-on-error`»**: hacerlo dejaría el job **rojo perpetuo por deuda de cobertura**, y un rojo perpetuo se ignora — que es exactamente donde estamos, sólo que con otra cara. El WP es **decidir qué vigila CI y hacer que vigile eso**: el conjunto de rojos **por nombre** es hoy estable y falla en las dos direcciones, así que es un gate honesto; la cobertura es **deuda con trinquete**, no acantilado. Separar las dos cosas y declarar cuál bloquea. **✎ ENTREGADO 2026-08-01** (rama `wp/v93-ci-vigila`, reporte `plan/REPORTES/WP-V93-ci-vigila.md`; **2ª vuelta tras devolución de 4 bloqueantes**): el paso blando se parte en **cuatro pasos que bloquean los cuatro** — gate de rojos por nombre · suite instrumentada · trinquete de cobertura · guarda de que no vuelva un paso blando. **Cero `continue-on-error` funcional en todo `.github/workflows/`**, y ahora **vigilado por el propio flujo**. **El eje cambió en la 2ª vuelta**: la 1ª entrega puso el trinquete como PORCENTAJE en `coverageThreshold` y lo vendió con «colchón de 1 punto» — **falso, y medido**: jest compara con el `pct` ya redondeado a dos decimales (`CoverageReporter.js:324`), así que un déficit de **0,01** bloquea y el colchón real eran **6 sentencias de 5903**. Y el **denominador no es estable**: 3 ficheros de `src` son código real que no compila (TS2353), no se instrumentan, y al entrar al mapa el total crece **+135 sentencias / +67 ramas con lo cubierto idéntico** — o sea que **arreglar dos errores de tipos ponía CI en rojo** (MEDIDO: 3 de 4 métricas caían bajo el suelo viejo). Solución: fuera `coverageThreshold`; el trinquete pasa a `scripts/cobertura-trinquete.mjs` y compara **unidades cubiertas absolutas** —inmunes al denominador— contra `scripts/cobertura.suelo.json` (**statements 1541 · branches 545 · functions 272 · lines 1519**, medidas hoy; colchón **0**), **falla en las dos direcciones** (si baja y si sube sin registrarse) y añade un **censo**: ningún fichero de `src` puede salir del mapa sin declararse, ni seguir declarado si vuelve. **9 vectores MEDIDOS en local con el comando exacto de CI**: rojo nuevo, rojo que desaparece, cobertura que baja, cobertura que sube, censo en sus tres direcciones, informe rancio y guarda de argumento. **Prueba de que el nuevo eje es el correcto**: contra el mundo en que los 3 ficheros sí compilan, el trinquete **no se mueve** (cubiertas idénticas) y el censo señala los tres por su nombre con la corrección de una línea. `npm test` sale **0**. Coste: la porción de test pasa de **una** corrida de suite a **dos** (~60-100 s → ~170 s en esta máquina; runner **no medido**). **Sin vigilar y señalado**: `release.yml` no corre ni un test **y `ci.yml` no se dispara con tags**, así que un `push` de tag publica un `.vsix` de un ref cuya suite nunca corrió · eslint no mira `scripts/` ni `tests/` · el trinquete no tiene tests propios · suelo calibrado en **Windows/node 22** y CI usa **ubuntu/node 20** (dos ejes, **ninguno medido**) · «bloquea» está probado sobre el **job**, no sobre el **merge**: que `ci` sea comprobación obligatoria es configuración de servidor, no medible desde aquí | el gate corre en CI y **bloquea** · un rojo nuevo tumba el job (probado en PR o con vector) · la cobertura no bloquea pero **no puede bajar** · cero pasos con `continue-on-error` sin razón escrita | **P0** |
| **WP-V94** **`P0`** ✅ | **CI de V está ROJO por dos errores de lint en código de producto**, y lleva así desde antes de la ola 6 — hallado por el orquestador al cerrar, **la primera vez que miró el CI de este mundo** (`gh run view 30714794390`, job `build`, paso `Lint (eslint · src)`): `src/core/errorBoundary.ts:61:35` y `:63:33`, `no-control-regex` — caracteres de control en expresiones regulares (`U+0009,U+000A,U+000A` y `U+0000,U+0000`✎ transcritos: la fila llevaba los bytes de control CRUDOS, que partian el renglon en tres y hacian el fichero ilegible como texto para grep/rg; los rangos reales estan en el reporte de V94). **No lo introduce V93**: los tres runs anteriores del remoto también salen `failure`. **Por qué es P0 y no cosmético**: V93 acaba de hacer que CI **bloquee** —dos pasos que fallan de verdad, cero marcas blandas, guarda contra su regreso—, y todo eso **no sirve de nada mientras el job caiga antes por otra puerta**. Un CI rojo permanente se ignora igual que uno blando; es el mismo defecto que V93 cerró, entrando por el lado del lint. **La decisión es parte del WP** y hay que medirla antes: esas dos expresiones regulares **posiblemente sean correctas** —sanear entrada suele exigir nombrar caracteres de control— así que la salida puede ser una exención declarada por línea con su razón, no reescribir el código. Lo que **no** vale es apagar la regla para todo el repo. **✎ ENTREGADO 2026-08-01** (rama `wp/v94-lint-produccion`, base `7d8e39b`, reporte `plan/REPORTES/WP-V94-lint-produccion.md`). **Primer hallazgo: el fichero de esta fila es el equivocado.** Los dos errores NO están en `src/core/errorBoundary.ts` sino en **`src/webview/security.ts`** — obra de V66. `errorBoundary.ts` tiene **3 warnings y CERO errores** (`18:12` no-explicit-any, `211:17` y `224:17` no-case-declarations, verificadas una a una contra mi árbol), y su **única** regex es `/[^a-zA-Z0-9]/g`, que no casa ningún control. El log agrupa por fichero y la fila tomó el encabezado equivocado: entre el de `errorBoundary.ts` y las dos líneas `##[error]` hay **25 ficheros más**. **Las líneas 61/63 sí eran exactas** — pero de otro fichero. **Las dos regex son CORRECTAS y son superficie de seguridad**: transcriben el preprocesado del *basic URL parser* de la WHATWG (`[U+0009,U+000A,U+000D]` = «remove all ASCII tab or newline», borrado en cualquier posición; `[U+0000‥U+0020]` = «remove leading and trailing C0 control or space»), y son la puerta que cierra el defecto D-1 de V66 — sin ellas `htt<TAB>ps://evil` se clasificaba como **relativo** y el navegador lo cargaba como https externo. **Se auditó lo único que sí podía ser defecto**: el código aplica las dos pasadas en orden **inverso** al de la norma; demostrado equivalente (`T⊂C`, así que borrar antes sólo destapa más C0 en los bordes que el recorte posterior se lleva) **y medido** como test con 1000 entradas, 0 divergencias. **Vía elegida: exención POR LÍNEA con su razón al lado** — las **dos primeras** del árbol (antes `grep -rn eslint-disable src/` = **0**); `.eslintrc.cjs` **no se toca**. **Precio de la descartada, MEDIDO y no supuesto**: reescribir a `/[\t\n\r]/g` y `/^[\0-\x20]+…/g` **sí funciona** y es equivalente carácter a carácter (0 divergencias en U+0000‥U+02FF) — pero el silencio lo da la **ORTOGRAFÍA, no el contenido**: los MISMOS caracteres escritos `/[\x09\x0a\x0d]/g` **vuelven a dar error** (`no-control-regex.js:30-40` dispara por byte crudo o por prefijo `\x`/`\u`, y `\t`/`\n`/`\0` no están en esa lista). Comprarlo así borraba del fichero todo rastro de que hubo una regla y dejaba el rango exento **para siempre**, aunque alguien lo ensanchara después. **Vector de que la regla sigue viva** (`tests/unit/webview/noControlRegexVivo.test.ts`, 5 casos, corre ESLint de verdad): una regex de control **nueva en ESE MISMO fichero** se caza con severidad **error** (la exención no ensancha a fichero) · **quitando los dos comentarios vuelven exactamente 2 errores** `\x09` y `\x00` (o sea: el código no se ha «arreglado» deletreándolo de otra forma) · la regla es error en cualquier otro fichero de `src` · **cero** `eslint-disable` de bloque o sin regla nombrada en todo `src`. Y **6 tests** que fijan el comportamiento de las regex, incluida la trampa que un arreglo perezoso rompería: **U+000B y U+000C se CONSERVAN** en posición interior (son C0 pero **no** son «ASCII tab or newline»; borrarlos divergiría del navegador **por exceso**). **Hermanos — la pregunta tenía dos respuestas y la importante es la segunda**: detrás del primer error de lint, **ninguno** (eslint no para en el primero: `184 problems (2 errors, 182 warnings)` en CI **y** en local, idénticos); pero detrás del **PASO** de lint había **OCHO pasos que nunca llegaron a correr** — `Compile production`, `Probe V08`, y los cuatro que V93 acababa de montar (`Gate de rojos`, `Suite instrumentada`, `Trinquete`, `Guarda de pasos blandos`), más `Package v1` y `Upload artifact`. **Todo el aparato de V93 no se había ejecutado ni una vez en el remoto.** Los **ocho** corridos en local con el comando exacto del flujo: **exit 0 los ocho**. Lint `184 → 182 problemas` (**0 errores**) con el **recuento de warnings intacto** — no se silenció nada de paso. Suite **411 → 422** tests, gate `IDENTICO`, **trinquete inmóvil** (1541/545/272/1519, exactamente el suelo: los 11 tests nuevos tocan funciones ya cubiertas y el fichero del vector no importa `src/`); `cobertura.suelo.json` y `rojos-jest.baseline.txt` **sin tocar**. Correrlo **no ensucia rastreados** (medido antes/después). **Corrección a esta fila**: no son «los tres runs anteriores» sino **SEIS** rojos consecutivos; el primero es `30675762405` y el último verde `30674820317`, ventana que señala al merge de **V66** — correlación, **no** `git bisect`. ⏳ **Lo único que falta, y no lo cierra el worker**: la CA «`gh run view` sobre `main` da `success`» exige push+merge, reservados al orquestador; **la rama no se ha empujado** y no hay run que enseñar. Ejes no medidos que se heredan de V93: local es **Windows/node 22**, CI es **ubuntu/node 20** | `gh run view` sobre `main` da `success` · cero marcas blandas nuevas · si se exime, la exención es **por línea y con razón escrita**, y un vector demuestra que la regla sigue viva para el resto | **P0** |
| **WP-V95** **`P0`** ✅ | **El test de portabilidad de V91 falla EN CI — y falla porque nunca se ejecutó fuera de Windows.** Destapado al empujar V94: el paso del gate cae con `+ FALLA scripts/tests/rojos-jest.test.ts :: WP-V91 · … RAÍZ AJENA · un informe hecho en CI da la MISMA línea que uno hecho aquí`. **V94 no lo rompe: lo hace visible**, porque hasta ahora el job moría antes en el lint y *todo el aparato de V93 no se había ejecutado ni una vez en el remoto*. **Causa, diagnosticada por el orquestador**: la fixture (`rojos-jest.test.ts:374-384`) simula «informe venido de CI» con la ruta `/home/runner/work/V_SDK/V_SDK/tests/unit/ci.test.ts`. En Windows esa ruta **es ajena**: `path.relative` devuelve algo que empieza por `..`, se toma la rama de reserva (`rojos-jest.mjs:126-129`) y mutarla rompe la aserción, que es lo que el arnés de mutación exige. **En CI esa ruta NO es ajena: es la raíz real**, `path.relative` tiene éxito, **la rama de reserva no se ejecuta jamás**, el mutante sobrevive y el test cae. **La lección, que va al método**: *una fixture que simula «el otro entorno» deja de simular nada cuando corres en el otro entorno*. Su contrarrevisión ya lo había avisado sin poder medirlo — «sobrevive en Windows por ser inobservable aquí, no por hueco demostrado». **Arreglo**: que la ruta de la fixture sea **ajena en las dos plataformas** —ni la raíz de Windows ni la del runner— y **que el test lo demuestre en ambas**, no que lo suponga. **✎ ENTREGADO 2026-08-01** (rama `wp/v95-fixture-ajena-de-verdad`, reporte `plan/REPORTES/WP-V95-fixture-ajena-de-verdad.md`). **Causa confirmada, no redescubierta** (`gh run view 30717696234`: una línea, una sola, en todo el conjunto). **El defecto es de la fixture; `scripts/rojos-jest.mjs` NO se ha tocado**, ni `scripts/rojos-jest.baseline.txt` — este WP devuelve un verde a verde, no legaliza un rojo (`--gate` → `IDENTICO`, exit 0). **Lo que cambia**: la raíz ajena se **deriva** de `RAIZ` (hermana del árbol, jamás descendiente: ajena por construcción en las dos plataformas) en vez de ser la constante `/home/runner/work/V_SDK/V_SDK`; `correr()`/`elMutanteDebeCaer()` reciben **`cwd`**, porque el instrumento saca su raíz de `process.cwd()` y **el cwd de la invocación ES la condición bajo prueba**; el test **demuestra** la ajenidad con el criterio literal del instrumento antes de aseverar nada —si se pierde, avisa por su nombre con las dos rutas delante en lugar de disfrazarse de «mutante superviviente»—; y el mutante se mata en **2 raíces × 2 estilos de separador** (los backslash son nuevos: la rama de reserva parte por `[\\/]` y sólo se probaba con barras). **Test nuevo** que deja el defecto escrito: con la fixture BAJO la raíz del proceso, el mutante **sobrevive** — 36 → 37 tests. **ROJO ANTES / VERDE DESPUÉS EN LAS DOS CONDICIONES**, con jest real: condición A (raíz = este árbol) 36→37 passed; condición B (raíz = ancestro de la fixture, montada copiando el árbol a una raíz con forma de runner) **1 failed / 36 antes**, **37 passed después**, y el rojo es literalmente el de CI. **BARRIDO DE LAS 36, seis ejes medidos**: **(a)** sólo **1** tenía el vicio de V95; **(b)** **0** dependen de dónde vive el árbol (suite completa con `RAIZ` movida); **(c)** **4 de §4** suponen que ningún ancestro de `os.tmpdir()` se llame `tests` — MEDIDO con `TEMP=…\tests\tmp`: 4 rojos, y son esos cuatro (señalado, **no arreglado**); **(d)** **1** supone ≥ 2 CPU (`PARALELISMO · porcentaje`: con 1 CPU su propio control se rechaza); **(e)** **1** supone ICU con collation (`ORDEN CANÓNICO`: sin ella el mutante `localeCompare` sobreviviría — discrimina en los 5 locales probados); **(f)** **14 son más estrictas en Windows que en POSIX** — MEDIDO: cada mitad de la normalización de separadores es individualmente redundante (37 passed quitando cualquiera), pero quitando las dos caen 14; **es el espejo exacto de V95 en la dirección buena** (sin falsos rojos). **Hueco de producción destapado y NO tocado por estar fuera del alcance**: en Windows `path.relative` entre **unidades distintas** devuelve la ruta absoluta, no `..`, así que un informe de otra unidad no se detecta como ajeno. **Sin cubrir**: no he corrido nada en Linux —reproduzco la condición, no la plataforma—, así que la comprobación de verdad es el próximo run del remoto | el gate **verde en CI** leído con `gh` · la fixture es ajena en las dos plataformas, con el mutante cayendo en las dos · barrido de las demás fixtures buscando el mismo vicio | **P0** |
| **WP-V97** **`P0`** ✅ | **ACEPTADO 2026-08-02** (rama `wp/v97-publicar-con-red`, obra `bf957ee`, merge `ff02fc7`; contrarrevisión + verificación del orquestador, CI de `main` en verde leído con `gh`). El hueco, medido mecánicamente antes de tocar nada: **8 comandos** que `ci.yml` ejecutaba y `release.yml` no (lint, probe V08, gate de rojos, suite, trinquete, guarda anti-blandas y los dos modos del arnés exthost). **Y el dato del remoto que lo cierra**: el único release de la historia del repo (run `30158829091`, tag `v0.1.0`) publicó el `.vsix` a las `12:57:45Z` y el `ci` de **esa misma revisión** no concluyó hasta las `12:58:02Z` — **17 s de artefacto descargable sin veredicto**; salió verde por suerte, nada lo condicionaba. **Vía elegida: reejecutar** la cadena entera dentro de `release.yml`, antes de empaquetar y de publicar (21 pasos; la verificación va del 11 al 19, publicar es el 21). **Precio de la descartada** —exigir que el ref etiquetado ya tenga un run verde, **0,8-1,1 s** medidos frente a **85 s**—: tres modos de fallo, la carrera que el propio `v0.1.0` demuestra, la etiqueta sobre una revisión que el remoto no conoce (`422 No commit found for SHA`, medido) y que comprueba un **registro** y no el árbol que empaqueta; su ahorro acumulado a día de hoy serían **84 segundos, en una publicación**. **Coste: ~40 s → ~2 min 5 s** (×3,1 · proyección por suma de pasos de runs reales del mismo runner; el `ci.yml` completo son 103 s de reloj con dos jobs en paralelo). **Vector que bloquea**: sobre la revisión REAL `6f792c7` (ci `30702323323` en rojo por lint) el flujo viejo produce `aleph-0-0.2.0.vsix` de 294 244 bytes y el nuevo muere en el paso 11 de 21, con la caída idéntica **línea a línea** a la que registró el runner. **Vector que no paraliza**: 10 pasos verdes sobre el árbol de la rama —el rc=1 del trinquete es el delta Windows/Linux ya firmado en `cobertura.suelo.json:14-18` → WP-V96, no una regresión— más los mismos textos de comando verdes en el runner (run `30719606823`, 14+7 pasos en success). **Cero marcas blandas nuevas**: la guarda anti-`continue-on-error` corre ahora también al publicar y barre `.github/workflows/` entero. Nuevo `scripts/verificacion-paridad.mjs`: exige que todo `run:` de `ci.yml` esté en `release.yml` **y antes del paso que publica**, falla cerrado, exclusiones firmadas dentro; su límite declarado es que sólo corre al publicar. **`ci.yml` no se toca**: la vía no exige disparar con etiquetas, razonado en §4.4. Sin demostrar: **no hay ninguna publicación real detrás** —no empujo y etiquetar crearía un release—, así que la primera etiqueta será la primera ejecución de este flujo (§7.1). Reporte: `plan/REPORTES/WP-V97-publicar-con-red.md` | publicar desde una revisión sin verde **falla** · el vector que lo demuestra · cero pasos blandos nuevos · el coste en minutos, medido | **P0** |
| **WP-V96** `P1` ✅ | **ACEPTADA 2026-08-02** (rama `wp/v96-suelo-portable`, obra `3c96cca`, reporte `491c10a`, merge `aa06442`) — **la rama existe, tiene nombre y esta cerrada**: `src/core/logging/redact.ts:267`, el **operando derecho** del `&&` en `process.env.HOMEPATH && process.env.HOMEDRIVE`. En Windows `HOMEPATH` existe, el `&&` no corta y el derecho se evalua; sin `HOMEPATH` corta y **no se evalua jamas**. Diferencia: exactamente UNA rama — y explica por que las otras tres metricas coincidian exactas, porque todo ocurre dentro de **una sola sentencia**, asi que statements/lines/functions no se mueven. **Ningun test fijaba esas variables: el numero lo decidia la maquina.** **El arreglo NO toca `src/`** (cero ficheros bajo `src/` en el diff): tres tests que imponen las cuatro variables, con lo que la rama se cubre igual en cualquier plataforma — y de paso vigilan **por primera vez** la composicion `HOMEDRIVE+HOMEPATH`, codigo de Windows que en CI no se ejecutaba nunca. **VERIFICADO POR EL ORQUESTADOR con su propia medida**, no leido: corrida completa con `HOMEPATH` y `HOMEDRIVE` **desarmadas** (la condicion del runner) → `Branches 25.18% (546/2168)`, 428 pass / 1 skip / 429, identico al suelo declarado. La dependencia de plataforma **ha desaparecido**. `cobertura.suelo.json`: **546**, un solo numero, sin nota de plataforma. Y con la frase que evita el malentendido futuro escrita donde se lee el numero: **546 no es un suelo relajado, es una rama que antes no vigilaba nadie**; el trinquete sigue mordiendo en las dos direcciones (545 → BAJO exit 1; 547 → SUBIO exit 1). **Vecinos**: 4 cerrados con medida (las 4 fixtures del temporal llamado `tests` — antes 4 failed/37, despues 40/40 — y el supuesto de >=2 CPU, mas **un segundo caso que V95 no habia censado**), 1 cerrado a medias con su razon (ICU: si `localeCompare` degrada, ninguna fixture distingue los dos comparadores; ahora **falla con su nombre** en vez de disfrazarse de mutante superviviente) y 1 enrutado (`path.relative` entre unidades de Windows, hueco de produccion). **Devuelta una vez por defecto de proceso**, no de obra: la entrego sin commitear. Suite 423 → 429, cero `.skip`/`.todo`/`xit` | la rama nombrada por fichero:linea ✓ · mismo veredicto en las dos condiciones ✓ (546/546, medido por el orquestador) · un solo numero sin nota de plataforma ✓ | P1 | **El suelo de cobertura mide distinto en Windows que en CI, y la ficha que lo iba a cerrar no existía** (✎ **fila escrita 2026-08-02 por el orquestador**: `scripts/cobertura.suelo.json:18`, `WP-V97-publicar-con-red.md:409` y `:473` enrutaban a «WP-V96» y **en este backlog no había ninguna fila con ese nombre** — una promesa firmada en un fichero de datos apuntando a un encargo inexistente). El hecho: el suelo se calibró en **Windows/node 22** y CI corre **ubuntu/node 20**; `statements`, `functions` y `lines` coinciden **exactamente**, y difiere **UNA rama** (545 local / **544** CI). Recalibrado a 544 por el orquestador en la ola 7 con la regla declarada *el suelo pertenece al entorno que lo exige*. **Consecuencia viva, no escondida**: una corrida **local** puede reportar «SUBIÓ» por esa rama y el trinquete falla en esa dirección a propósito — o sea que hoy el instrumento da un rojo que no es una regresión, en la máquina donde más se corre. Cerrarlo es **localizar la rama** y neutralizar su dependencia de plataforma, no volver a mover el número. **Vecinos ya censados por V95, enrutados aquí y no arreglados por él**: 4 fixtures suponen que ningún ancestro de `os.tmpdir()` se llama `tests` (MEDIDO: 4 rojos con `TEMP=…\tests\tmp`) · 1 supone ≥ 2 CPU · 1 supone ICU con collation · y en Windows `path.relative` **entre unidades distintas** devuelve ruta absoluta en vez de `..`, así que un informe de otra unidad no se detecta como ajeno | la rama nombrada por fichero y línea · trinquete que da el **mismo** veredicto en Windows y en CI, demostrado en las dos · `cobertura.suelo.json` con un solo número y sin nota de plataforma | P1 |
| **WP-V92** `P2` ✅ | **Tres citas rancias en reportes ajenos**, todas dejadas por V90 sin tocar (regla «un escritor por worktree»): `WP-V66:813` citaba un test del fichero que V90 **borró entero**, y `WP-V23:1375` y `:1385-1386`. Ninguna afecta a código; son deuda de trazabilidad. ✎ Regla que sale de la ola 4 (de U237-B3, misma clase): **una cifra o una cita «medida por grep» caduca — o se re-mide al citarla, o se cita el gate que la sostiene** | cero citas que apunten a ficheros o líneas inexistentes en los reportes vivos | P2 |
| **WP-V98** `P1` ✅ | **El CENSO esta vivo y miente en presente** (hallazgo E-1 de V92, verificado por el orquestador al aceptar). `plan/CENSO-V12.md` **no es un reporte**: es el censo vigente, y V14 y V15 trabajaron sobre sus filas. Afirmaba **en presente** que **el fichero del editor de configuracion** es «legado intacto y muerto: 423 lineas» (fila `src/configEditor.ts` de su Tabla B) y que **el de la barra de estado** tiene «453 lineas no alcanzables» (fila `src/statusManager.ts`) — **los dos ficheros ya NO EXISTEN**, podados por V13 en `c164731` (2026-07-25); comprobado por el orquestador con `test -f`. Arrastraba ademas la fila `src/terminalManager.ts`, que decia «lo usan processManager y statusManager» con el segundo ya borrado. ✎ **Esta fila la corrigio el orquestador el 2026-08-02**: citaba los dos ficheros por su ruta —lo que la hacia rancia por la regla de `PRACTICAS §7`— y anclaba dos coordenadas desnudas (`:167`, `:186`) al censo, **que se desplazaron al anotarlo**. Se citan ahora **por nombre de fila**, no por numero, siguiendo la observacion del propio worker: *un numero de linea nuevo nace con fecha de caducidad*. **ACEPTADO 2026-08-02** (obra `ccbbb45`, reporte `7db631d`, merge `90c4599`). **Veredicto: el censo se CIERRA COMO ACTA**, con banner en la primera linea y marco temporal global. No por estilo, por tres medidas: sus 23 podas estan **ejecutadas las 23** y sus decisiones cerradas en bloque; sus unicos consumidores son los briefs de V13/V14/V15, **los tres aceptados**; y censa **69 filas de un arbol que hoy tiene la mitad**, con **23 describiendo cosas borradas**. En palabras del worker: *«corregirlo no habria sido corregirlo, habria sido escribir otro documento»*. **Consecuencia declarada y no escondida: hoy el carril NO tiene censo vigente.** **El hallazgo desborda el encargo: eran 79 referencias defectuosas en TRES clases, y el barrido automatico solo ve una.** **(A)** no resuelve — **21 de 189**, las que ya sabiamos. **(B)** **resuelve y miente** — **24 de 33**, universo completo y no muestra; la peor, `jest.config.js:23-28`, resuelve y hoy contiene el comentario de V93 explicando **que no hay ningun umbral**: el hecho se invirtio y la cita sigue pareciendo buena. **(C)** clase **NUEVA y suya**: retro-referencias desnudas `(:NN)`, **invisibles al barrido por construccion** — **34 de 36 desplazadas**, y ni entran en el denominador. **Y de ahi sale un defecto de producto**: **D16 sigue vivo y la cita rancia lo tapaba** → `WP-V100`, verificado por el orquestador abriendo el fichero. **Dos apuntes de metodo**: hallo un **cuarto defecto del barrido de V92** y **decidio no arreglarlo** para no invalidar la comparacion (enrutado a `V99`); y **el barrido le cazo a el** — dos citas que introdujo al nombrar el defecto salieron rancias en la corrida siguiente, y lo conto. **Devuelto una vez por defecto de proceso** (obra sin commitear), como V96. 516/516 lineas originales conservadas literales, cero palabras borradas. ✎ **Las tres rancias que quedaban en esta fila y en la de V90 las corrigio el ORQUESTADOR**, porque son suyas: dos las sembre yo al describir este defecto, y la tercera es el acta de una poda — **toda fila que registre un borrado citara una ruta muerta**, y esa clase no es defecto sino limite del instrumento: anotada, no corregida. **Por que es P1 y no cosmetico**: la frontera que sale de V92 es que un reporte es acta y solo se anota, pero **un documento vivo se corrige, porque alguien planifica sobre el**. Este es el caso limite y esta del lado equivocado. El barrido de V92 corre tal cual con `AMBITO=plan` — el instrumento ya existe, falta aplicarlo y decidir por fila que sigue vigente | cero afirmaciones en presente sobre ficheros inexistentes · cada fila del censo re-medida o retirada con acta · el barrido de V92 con `AMBITO=plan` en verde | P1 |
| **WP-V99** `P2` ✅ | **ACEPTADO 2026-08-02** (obra `754969d`, merge `496371a`). El barrido vive en `scripts/citas-rancias.mjs` con **29 tests bajo `npm test`**. **Equivalencia con V92 demostrada, no inspeccionada**: corrió los dos sobre el mismo árbol en el mismo instante — veredictos **idénticos y con multiplicidad**, 198/198 y 38/38. **Las tres cegueras con caso rojo de verdad**: retiró cada corrección **del instrumento** → **6 / 12 / 10 rojos de 29**, y en los tres cae el test que lleva su nombre. Censo de mutación **14/14, cero supervivientes**, con la regla del cuadre declarada **en pareja a propósito** en vez de disimulada. **Corrigió la premisa del brief del orquestador con evidencia**: yo dije que el denominador crecería porque una ceguera dejaba 367 ficheros fuera; **falso** — el 1518 de V92 **ya los contaba**, demostrado con su propia tabla. Crece por otra razón y **cierra exacto**: 1625 → 1676 = **+51**, justo lo que aporta el reporte de V96. **Y re-midió el coste de cada ceguera HOY**: sobre `plan/REPORTES` ninguna produce ya rancia falsa, pero sobre `--ambito plan` **una de ellas esconde 4 rancias reales**. El modo de fallo de V92 **no es historia**. **El barrido midió que su propio encargo se cumplió**: mientras esa ruta no existió, la cita de V92 a ella era `EFIMERA`. **La ceguera irreducible se declara ahora en cada corrida, en `--help` y en la cabecera.** **Enrutado**: el gate **no tiene turno en CI** — `plan/REPORTES` ya podría bloquear, pero `--ambito plan` sale 1, y meterlo hoy sería **el rojo permanente que V93 quitó** | el barrido en `scripts/` con sus tests ✓ · las tres cegueras como casos rojos ✓ · la clase irreducible declarada **en el instrumento** ✓ · denominador siempre en la salida ✓ | **El barrido de citas no tiene casa, y sus tres cegueras estan medidas** (hallazgos E-3 y E-4 de V92). El instrumento vive **embebido en un reporte** porque el ALCANCE_DIFF de V92 no incluia `scripts/`. Tres errores propios ya censados y corregidos por su autor, que son la parte util: `js` casando antes que `json` (10 falsos positivos) · los ficheros de raiz **sin barra quedaban fuera del denominador** (367 citas sin mirar, 5 rancias ocultas) · el prefijo de mundo `z:` (12 falsos positivos, estuvo a punto de enrutar un defecto inexistente). Y la ceguera que **no se puede cerrar con este diseno**, con su caso: `WP-V90:357` cita `plan/BACKLOG.md:153` «que nombra duration < 100 ms»; la linea **resuelve** (215 lineas), pasa la verificacion, **y es falsa** — hoy la 153 es la fila de V68. Un verde sobre citas significa «ninguna apunta al vacio», nunca «todas dicen la verdad» | el barrido en `scripts/` con sus propios tests · las tres cegueras como casos rojos · la clase E-3 declarada en el propio instrumento, no en un reporte | P2 |
| **WP-V100** `P1` ✅ | **El defecto D16 no se arregló: se movió, y la cita rancia lo tapaba** (hallado por V98; **verificado por el orquestador abriendo el fichero**). El censo marchó D16 como falsedad silenciosa en `mcpConfigurationManager.ts:58-65`; esa coordenada **hoy resuelve y contiene código inocente**, así que el barrido de citas la da por buena. El defecto vive intacto en **`src/core/mcpConfigurationManager.ts:42-49`**, y **miente dos veces sobre el mismo hecho**: el comentario dice «busca `sample-config.json` en el workspace», la línea siguiente compone la ruta de **`ArrakisTheater_OperaConfig.json`**, y el log escribe `Found sample-config.json at: ${configPath}` mostrando la ruta del **otro** fichero. Agravante: **ese fichero lo podó V13**, o sea que la rama busca en disco algo que ya no debería existir y, si existiera, lo anunciaría con otro nombre. **Por qué importa más que su tamaño**: es el caso límite de la clase B de V98 — una cita que **resuelve** y cuyo **contenido es falso** — aplicada a un defecto de producto, no a un reporte. Ningún gate lo ve: el barrido pasa la coordenada, la suite no ejerce esta rama, y el log sólo miente a quien lea la consola. **La ruta original decía `WP-V16`, que NO EXISTE** — enrutamiento a la nada, la misma clase que ya nos dejó tres hallazgos huérfanos en el motor. Ficha viva abierta por el orquestador el 2026-08-02  ✅ **ACEPTADO 2026-08-02** (obra `32ce06f`, reporte `0a8a7f3`, merge `45649b4`; **CI verde en la rama antes de fusionar** (`0a8a7f3`) **y en el commit de merge**; **contrarrevisión sin bloqueantes**). **LA DECISIÓN, Y ES MÁS ANCHA QUE EL ENCARGO**: el brief pedía alinear tres descripciones al nombre real. El worker hizo eso **y una cosa más, declarándola como desviación**: *«re-sincronizar tres copias a mano deja intacto el mecanismo que las desincronizó»*. El nombre pasa a ser **una constante `OPERA_CONFIG_FILENAME`** que los tres sitios ejecutables interpolan, así que **el log y el aviso ya no PUEDEN divergir del fichero que se abre: son la misma expresión, no cadenas gemelas.** Su argumento es histórico y no retórico: *es literalmente lo que ya pasó* — D16 se dio por cerrado y reapareció quince líneas más abajo. **Corrigió el brief al alza**: eran **tres** menciones muertas contra dos vivas, no dos — la docstring de L34 también mentía, y el brief anterior sólo citaba dos. **Y cerró la puerta a la alternativa con dato**: «buscar el que dice el comentario» **no era una opción disponible**, porque V13 podó `sample-config.json` **y** `ArrakisTheater_OperaConfig.json` en el mismo commit (`f615434`) — era **elegir entre dos fantasmas**. La búsqueda apunta al **workspace del usuario**, no al repo, y por eso la poda **degrada en silencio y no rompe**, exactamente como ya lo registraba el censo. **EL TEST NO LEE EL FUENTE: EJECUTA.** §1 corre `initialize()` de verdad, captura la ruta que llega a `fs.existsSync`/`readFileSync` y el mensaje que llega al logger, y compara **basename contra basename**. *Eso ES D16.* Antes de este WP, `tests/` tenía **12 ficheros y ninguno nombraba el manager**. **LOS NEGATIVOS, DESACTIVANDO SU GUARDIÁN — y el primer intento fue un FALSO VERDE que él mismo cazó y contó**: el script pasaba a `node` una ruta POSIX que Windows resolvía como `C:\c\S_LAB\…`, **las mutaciones no se aplicaban y los cuatro casos salían verdes** — *un verde que sólo decía que no se había tocado nada*. Lo destapó porque el script imprime la línea mutada **antes** de correr jest; la versión final **aborta con código 3 si el patrón no está**. Las 4 mutaciones enrojecen (2/2/1/3 fallos) y el control restaura a 19/19. **M1 es la que importa: es el estado exacto del fichero antes de este WP.** **EL HALLAZGO HEREDADO: CERRADO, no enrutado** — y re-medido al alza: el brief decía «8 citas, 4 falsas»; son **9 coordenadas y 5 falsas**, con una que no es un desplazamiento sino **otro sitio entero** (`socketMonitor.ts:643` es JS de la webview; lo real es `:308`). **Las nueve RESUELVEN**, y ése es el punto: *un barrido que sólo comprueba que el fichero tiene esa línea las aprueba todas*. El test fija **11** coordenadas **con lo que cada una debe nombrar**, no sólo que resuelva. **EL SUELO SE MUEVE Y ESTÁ FIRMADO CON CUATRO CORRIDAS, no con una**: la corrida A existe **para probar que el suelo declarado reproduce en este árbol antes de moverlo** (1816/547/352/1780, exacto) — sin eso el delta no significa nada. **Y la corrida B no es decorativa**: aísla **+1 sentencia que NO es test** — la declaración de la constante, que se ejecuta al importar — *que sin B se habría vendido como cobertura nueva*. Nuevo suelo **1844/558/357/1808**: sube en las cuatro, **no es un ablandamiento**, y con él **el informe de ayer se rechaza por regresión**. Censo intacto: los mismos 9 ausentes declarados. **Convención nueva y comprobable** — *un nombre muerto se escribe entre comillas angulares «…»; uno vivo va con la constante* — **con su debilidad declarada**: alguien podría silenciar §2 envolviendo un nombre vivo equivocado; **§1 lo cazaría igual, porque ejecuta**. *La convención protege la prosa; la ejecución protege la conducta.* **Y DEJÓ LA SIGUIENTE PIEDRA CON LA COORDENADA YA RE-MEDIDA** (→ `WP-V102`): `HackerConfigPanelProvider.ts:239` **no es prosa, es dato vivo** — el panel SETTINGS **ofrece `sample-config.json` como plantilla al usuario**, y el propio censo lo citaba en `:233` (**+6, otra cita de clase B dentro del censo mismo**). Su frase: **es MÁS GRAVE que el defecto que este WP venía a cerrar.** **Cero regresión**: 14 suites/478 → **15 suites/497** (496 pass, el mismo skip de siempre), `tsc --noEmit` **0 errores en el fichero tocado** | el nombre que el log anuncia y el fichero que abre son el mismo ✓ (**y no por sincronía: por interpolación de una sola constante — ya no pueden divergir**), con test que enrojece si divergen ✓ (**en ejecución, no leyendo el fuente**; M1 reproduce el defecto exacto y caen 2) · decidido y escrito qué fichero debe buscar ✓ (**con el dato de que los DOS candidatos están podados**, o sea que la alternativa no existía) · censo de las demás ramas ✓ (**con denominador y dos reglas, estrecha y ancha**; y las 2 fuera de alcance entregadas re-medidas, no sólo señaladas) | P1 |
| **WP-V101** `P2` ✅ | **Dos entradas del editor personalizado apuntan a un directorio podado** (hallado por V98, sin cerrar). Los dos `customEditors` conservan `viewType` `alephscript.*` apuntando a `theatrical-content/`, **podado**. Y en el mismo censo quedó la única fila *re-contenido* **sin ejecutar**: `schemas/`. Son deuda de coherencia entre lo declarado y el árbol, de la misma familia que `WP-V25` (comandos que prometen lo que no hacen) pero en la superficie de editores  ✅ **ACEPTADO 2026-08-03** (obra `e123005`, bloqueantes `6343661`, cierre `2157c48`, merge `aed096d`; **CI verde en el tip exacto `2157c48` Y en el commit de merge**; **2 contrarrevisiones**: 3 bloqueantes → 2 condiciones → **cero**). **LA FICHA ESTABA MAL EN LO QUE DECIDÍA EL WP, Y EL BRIEF SÓLO CORRIGIÓ LA MITAD.** `theatrical-content/` **no es convención podada**: la **escribe** el comando vivo `aleph0.agents.createNew` (`agentManagementCommands.ts:46`, `createDirectory`+`writeFile`) en el workspace del usuario — V13 podó la copia de ejemplo del repo, no la convención, así que el selector señalado **apuntaba exactamente a lo que producimos**. **El defecto real era el OTRO selector**: `*.agent.md` a secas con `priority: "default"`, que **reclamaba cualquier `.agent.md` del usuario en cualquier carpeta — incluido `node_modules/` y `.git/`** (regla de basename, **confirmada en el bundle del VS Code instalado**). Estrechado. **Y LA VERIFICACIÓN SE HIZO CONTRA LA AUTORIDAD, NO CONTRA UN SUSTITUTO**: el revisor extrajo `globMatchesResource` y el motor de glob del VS Code de esta máquina, los portó literalmente y **ejecutó el handler real**; el selector nuevo abre los dos ficheros en raíz, anidado profundo, con espacios, con mayúsculas y bajo `vscode-remote`/`vscode-vfs`. **Cero regresión de producto.** **LOS `viewType` HUÉRFANOS DE VERDAD, PODADOS** — no los del manifiesto: `theatrical.agentContentEditor` y `theatrical.agentConfigEditor`, en `static register()` **que nadie llamaba**; el artefacto literal del título de la ficha, y la ficha no los nombraba. **Poda verificada POR EJECUCIÓN**: compilados los dos bundles y ejecutado `activate()` contra un `vscode` de mentira → `main` y la rama registran **el mismo conjunto exacto** (2 customEditors, 5 webviewViews, 7 treeViews, cero errores), `theatrical.*` **0 antes y 0 después**; en el bundle de `main` los muertos **sí viajaban** (`registerCustomEditorProvider` ×3, hoy ×1). Y `git log --all -S` prueba que **esa llamada no existió jamás en ningún commit**, así que ningún usuario pudo tener una asociación guardada: el riesgo que planteó el orquestador **era vacío por construcción**. **`schemas/` RETIRADA CON ACTA: se quedan** — la premisa del censo era falsa, `configsTreeView.ts` (vivo, cableado en `assembleContext.ts:22`) vigila, escanea y **genera** los tres nombres. **Y AL MEDIRLO SALIÓ UN DEFECTO VIVO QUE NO ESTABA EN NINGUNA FICHA**: `createFromTemplate` podía persistir `url: ""` en un campo que **nuestros propios schemas declaran `required` con `pattern: "^wss?://"`** — *escribíamos en el disco del usuario un fichero que nuestro propio schema marca en rojo*. Cerrado con constante; verificado por los dos lados con `ajv` (el caso legítimo sin url, `webrtc-ui-config.json`, tiene `required: []` y **el arreglo no lo toca**). **EL GATE DE CA-3 ANCLA EL HECHO Y DERIVA LA COORDENADA** (`plan/ANCLAS.json` + `scripts/anclas-censo.mjs`): token, fichero y **cuántas veces**; el campo `veces` es lo que caza la deriva **de composición** — el censo declaraba 5 puntos en `extensionBootstrap.ts`, que hoy tiene **cero**. Son **cuatro** generaciones de deriva, no tres. **Y ENTRA EN CI DE VERDAD**: su §5 corre el instrumento **sobre el árbol real exigiendo PASS**, cosa que `citas-rancias.test.ts` **no** hace — no es otro guardián suelto de los de `WP-V103`. **LA MEJOR PRUEBA DEL INSTRUMENTO NO LA FABRICÓ NADIE**: su primera deriva real la provocó **`WP-V102` en paralelo** reescribiendo el panel, y saltó sola por la cadena `gate → test §5 → npm test → CI`. **LOS TRES BLOQUEANTES DE LA 1.ª VUELTA, Y DOS ERAN DEL ORQUESTADOR**: despaché V101 y V102 en paralelo sobre el mismo mundo **y el mismo trinquete** — V102 entró declarando `branches: 563` y V101 llegó con **562**, o sea que el merge **habría BAJADO una rama**, justo lo que el trinquete existe para impedir; y el ancla A8 **caducó en vuelo**. *Modo de fallo de CARRERA, no de medición: dos WP firmando el mismo dato sobre bases distintas.* Rebasado y re-firmado **sobre `main`**: nuevo suelo **1960/567/377/1921**, con corrida A que reproduce `1888/563/367/1850` exacto. **LA BAJADA DE −2/−2 DE LA CORRIDA B SE ATRIBUYÓ MIDIENDO, NO RAZONANDO**: restaurando **sólo** los dos editores desde `main`, la corrida vuelve **exacta**; el mecanismo es que desaparecen dos inicializadores de campo estático (que se ejecutan al importar) mientras los cuerpos de `register()` eran funciones **no cubiertas** — `functions.covered` 5→5, `total` 16→15. **No hay bajada sin nombre.** **EL TERCER BLOQUEANTE ERA SUYO Y ES DE LOS BUENOS**: el registro de anclas **se desarmaba borrando filas** — el revisor quitó 7 de 8, la suite siguió verde y **el gate dejó de cazar la regresión que este mismo WP arregla**. Cerrado primero con `>= 8`… que impedía **borrar** y no **vaciar** (misma fila, mismo `veces`, `debeNombrar` diluido → `PASS`), y cerrado de verdad **fijando el conjunto exacto por nombre y contenido**, la línea de defensa que ya usa `rojos-jest.baseline.txt`. **LA CONDICIÓN QUE MÁS ENSEÑA**: el test §4 se llamaba *«ningún fichero de `src/editors` nombra un `viewType` que el manifiesto no declare»* y su regex **sólo casaba los dos nombres recién podados**; el revisor metió un huérfano con otro nombre **en el fichero exacto donde vivía el defecto** y §4 **no disparó** — saltó el gate de anclas por desplazamiento de cita, **rojo que parece verificación y no lo es**. Ensanchado a **regla de clase** (cualquier literal con forma de identificador punteado en `src/editors/`, medido antes: **cero** falsos positivos hoy) más un flanco nuevo: **sólo `viewRegistry.ts` puede registrar customEditors**, recorriendo `src/` entero. **Y CORRIGIÓ SU ÚNICA FRASE CÓMODA con dato ajeno**: decía que migrar la tabla de `WP-V100` rendía poco porque «la mitad citas tiene el mismo coste»; **falso para esa tabla**, que pincha sus 11 coordenadas **dentro del test** con **cero** referencias a documento vivo. Lo que la deja fuera es el **alcance**, no el beneficio. Suite 16/507 → **18/552**, 0 fallos; `tsc` 8 errores preexistentes, ninguno en fichero tocado. **Límites declarados**: un `viewType` inerte **fuera** de `src/editors/` que nadie registre sigue invisible (inofensivo por construcción); el conjunto de anclas protege del vaciado pero **no descubre hechos que nadie ancló** — y el instrumento lo imprime en cada corrida; suelo en una sola plataforma; `citas-rancias` sigue FAIL con **3 preexistentes** enrutadas a `WP-V103`, con el off-by-one 1197/1198 reconciliado (`citas-rancias.mjs:248,260` usa `split('\n').length`) | cero `viewType` que apunte a ruta inexistente · la fila `schemas/` ejecutada o retirada con acta | P2 |
| **WP-V102** `P1` ✅ | **El panel SETTINGS ofrece al usuario un fichero PODADO como plantilla — y esto no es prosa, es DATO VIVO** (hallado y **re-medido** por `WP-V100`, que lo dejó con la coordenada corregida para que quien lo coja no repita el trabajo; palabras suyas: *«es MÁS GRAVE que el defecto que este WP venía a cerrar»*). `src/views/HackerConfigPanelProvider.ts:239` **ofrece `sample-config.json` como plantilla**; `sample-config.json` lo podó V13 en `f615434`. **La diferencia con D16 es de clase, no de tamaño**: D16 mentía en prosa y en un log que sólo lee quien abre la consola; **esto se le enseña al usuario en la interfaz**. Resuelve contra el workspace del usuario (`:234`) y filtra con `fs.existsSync` (`:247`), así que **degrada y no rompe** — ofrece un fichero que **nunca aparecerá**. **Y arrastra una segunda**: `src/mcpTypes.ts:15` describe *«the structure of sample-config.json»*, inerte pero falso. **Agravante de método, y es la razón de la P1**: **la coordenada del censo había derivado sola** — `plan/CENSO-V12.md:383` la fija en `:233` y hoy está en `:239` (**+6**), o sea **otra cita de la clase B de V98 dentro del censo mismo**, que es justo el documento que se cerró como acta. Ficha abierta por el orquestador el 2026-08-02 al aceptar `WP-V100`  ✅ **ACEPTADO 2026-08-02** (obra `efc96b1`, test+suelo `8b50fea`, reporte `af9d243`, acotación `79b4f71`, merge `5e25bbf`; **CI verde en el tip exacto `79b4f71` Y en el commit de merge**; 1 contrarrevisión: **ENTRA CON CONDICIONES, cero bloqueantes**). **EL DEFECTO NO ERA EL DE LA FICHA, Y LO MIDIÓ ANTES DE TOCAR NADA.** Ejecutó el panel con **dos workspaces reales en disco**: con este repo abierto 3+3+0; **con un proyecto ajeno, 0+0+0**. Los tres `schemas/*.schema.json` **viajan en el paquete** y el panel los buscaba bajo `workspaceFolders[0]`, así que no era «una plantilla muerta de cuatro»: era **un panel que resolvía activos del paquete contra el workspace**, y para cualquier usuario que no fuera el desarrollador de la extensión **no ofrecía nada en absoluto**. Y `_getWebviewConfigs()` **nunca aportó un elemento propio**: un fantasma más **tres duplicados de `_getSchemaConfigs()` con la misma ruta absoluta**. Retirada entera; `_getSchemaConfigs()` enumera los dos orígenes de-duplicando y **sin ningún nombre de fichero codificado**. **LA DECISIÓN, ESCRITA**: no hay plantilla y el producto **no debe ofrecerla** — `WP-V23` ya cerró la superficie de configuración en `aleph0.*`, y una plantilla JSON abriría una segunda compitiendo con ella; reforzado con el dato de que la forma que este producto ya eligió es **generar** (`configsTreeView.createFromTemplate()`), no enviar un fichero de muestra. **LA CONTRARREVISIÓN CERRÓ DOS LÍMITES QUE EL WORKER DEJÓ ABIERTOS, en vez de darlos por buenos**: construyó el `.vsix` y lo abrió (`extension/schemas/` con los 3, **sin** `sample-config.json`, `ArrakisTheater_OperaConfig.json`, `theatrical-content/` ni `src/`), y corrió el panel con `_extensionUri` = raíz del paquete desempaquetado contra un workspace ajeno: **3 schemas donde antes había 0**. Además rehízo la corrida B (1844/558/357/1808, idéntica al suelo de V100), regeneró cobertura desde cero (**1888/563/367/1850 exacto**) y probó el trinquete **en las dos direcciones**; y escribió **4 mutantes propios** que el worker no había puesto, todos rojos por sección nombrada. **LA ÚNICA CONDICIÓN FUE DE PROSA, Y VA AL FONDO DEL WP**: *«no desaparece ni un elemento de la interfaz»*, escrito **en un comentario de fuente**, es falso para un usuario con **su propio** `sample-config.json` en la raíz (medido: `-1/+3`). **El worker la reprodujo antes de aceptarla** —tres formas de workspace— y al acotarla **movió el argumento de sitio**: la entrada no se retira porque el fichero no exista (eso sería descubrimiento), sino porque su `name`/`description` la ofrecían **como plantilla del producto**. Verificado por el orquestador: **todas las líneas tocadas del `.ts` son comentario — cero lógica, cero test**. **Y CORRIGIÓ DOS AFIRMACIONES SUYAS**: «ningún nombre puede quedarse rancio porque no hay nombre» era ancha — `'schemas'` y `'.schema.json'` **sí** caducan, y el hueco **lo tapa la ejecución de §1, no la inexpresabilidad**; y la de-dup **se rompe por caja de la unidad** en Windows (declarada, no arreglada, con razón escrita). **Lo mejor del cierre**: al añadir la prosa de la corrección, **ocho coordenadas de su propio reporte derivaron** — las re-midió y verificó que las 10 citas **resuelven Y nombran lo que afirman**. *La clase B apareciendo dentro del documento que la denuncia, y cazada por su autor.* Límite que sigue abierto, más estrecho: **no se ha corrido en un Extension Host real** (`tests/exthost` sin usar) | lo que el panel ofrece existe, o **no se ofrece** · la decisión escrita (qué plantilla debe haber, dado que las dos históricas están podadas) · test que **enrojece si se vuelve a ofrecer un fichero que el árbol no puede producir** · `mcpTypes.ts:15` corregido o declarado muerto con la convención «…» de V100 | P1 |
| **WP-V103** `P1` ⬜ | **DOS GUARDIANES CONSTRUIDOS, ACEPTADOS Y QUE NO SE EJECUTAN NUNCA — y uno de ellos está ROJO ahora mismo** (hallado por el orquestador el 2026-08-02, disparado por una nota al margen de `WP-V102`, que reportó el rojo como preexistente y ajeno; **verificado corriéndolo yo en `main` limpio**, no leyendo su reporte). `scripts/citas-rancias.mjs` (obra de `WP-V99`, merge `496371a`) da hoy `VEREDICTO: FAIL (4 rancias / 1937 citas)`, `rc=1`, sobre `main` en `12be68e`. Las cuatro citan `package.json` **por encima de sus 1198 líneas**: `WP-V15:259`→`:1219`, `WP-V90:712`→`:1234`, `WP-V92:457`→`:1229`, `WP-V92:556`→`:1229`. **Y `scripts/verificacion-paridad.mjs` tampoco corre** (hoy sale `rc=0`, o sea que está verde **por suerte, no por vigilancia**). **Medido**: ninguno de los dos aparece en `package.json` ni en `.github/workflows/ci.yml`; de los cinco `scripts/*.mjs`, sólo `cobertura-trinquete.mjs`, `rojos-jest.mjs` y `vsix.mjs` están cableados. **El acta de V99 NO miente y ése es justo el punto**: dice *«29 tests bajo `npm test`»* — el instrumento **está probado**, y nunca **aplicado** al producto. Un instrumento probado y no aplicado pasa todas las revisiones y no vigila nada. **La ironía que lo cierra**: `verificacion-paridad.mjs` existe para comprobar que lo que exige CI se cumple al publicar — **es un guardián de CI que CI no ejecuta**. Es la primera cara de la regla de LORE-HM (*una defensa que no se ejecuta, una cifra que no se remide y una salida que no se recaptura son el mismo error con tres caras*) aplicada a nuestro propio árbol. **Cruce vivo**: las 4 rancias dependen del número de líneas de `package.json`, que `WP-V101` está tocando ahora — el rojo puede moverse solo. **⊕ DATO QUE CIERRA LA DISCUSIÓN DE SI DEBE CABLEARSE: en el mundo hermano YA ESTÁ CABLEADO.** `z-sdk/.github/workflows/ci.yml:39` corre `node scripts/verificacion-paridad.mjs` como paso propio; V tiene **el mismo script** (`scripts/verificacion-paridad.mjs`, 12428 bytes) y **no lo corre en ningún sitio**. No es que nadie pensara en ello: es que en un mundo se cableó y en el otro no. **Y el contraste completo, medido**: en Z busqué el mismo hueco y **no está** — `matriz-51.mjs` no entra por `npm run gates`, pero su test lo aplica al **repo real** (`test/gates/matriz-51.test.mjs:171`, `runMatriz51({repoRoot: REPO_ROOT})` con `assert ok===true`) y `test:gates` sí corre en CI. Aplicar el guardián por el arnés de test **cuenta**; tenerlo probado y nunca aplicado, no | los dos guardianes **corren en CI** y tumban la corrida cuando encuentran algo (con control positivo: fichero testigo que enrojezca) · las 4 rancias **cerradas por contenido**, no anotadas con ⛔ para silenciarlas · censo de `scripts/*.mjs` con **cuál corre, dónde y desde qué commit** — y ninguno más sin cablear · declarado qué NO cubren, porque el propio `citas-rancias.mjs` ya avisa de que `RANCIA=0` significa «ninguna apunta al vacío», nunca «todas dicen la verdad» | P1 |
| **WP-V49** `P1` | **Cerco en documentos**: referencias vivas a canales externos → sidecar inerte | cero anclas externas como dependencia |
| **WP-V80** `P1` ✅ | **Trocear `extensionBootstrap.ts`**: 2150 → **290 líneas** de flujo; DATOS en `src/core/bootstrap/` (16 módulos: tabla de 56 comandos por dominio + registry de 14 vistas). **La cadena serial V25·V62·V64·V71 queda habilitada como paralelo.** Base no compilaba limpia (8 err TS preexistentes ajenos) → invariante aplicado: misma salida EXACTA antes/después (tsc diff vacío · esbuild 0/0 · mismos 5 jest rojos por nombre), verificado en cada uno de los 7 commits. **Contrarrevisión adversarial PASS** (base regenerada por el revisor · 56/56 extracción propia mismo orden · 14/14 vistas campo a campo · captura-viva equivalente con citas · 286 literales rastreados globalmente · obs menor: trailing whitespace en 2 template literals, sin conducta). ⏳ verificación en host real la cubre el arnés V68 en CI. Aceptado 2026-07-31 (rama `wp/v80-troceo-bootstrap`) | bootstrap <300 ✓ (290) · misma salida exacta ✓ · dedup 1-definición ✓ · cero cambio observable ✓ (probado) |

## LANE H · EMPAQUETADO Y CANAL — **P0/P1**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V50** `P1` | **Guardas del release**: tag≠versión, check-then-act, `shell:true`, publish sin gates | tag falso **falla** · guardas probadas, no declaradas |
| **WP-V51** `P1` | **Gate de artefacto** (W-1): verificar contra el paquete real | `unzip -l` en el CA · las dos fugas históricas cubiertas por test |
| **WP-V83** `P1` | **Supply-chain del VSIX**: SBOM, checksums, audit de deps, provenance | vsix inspeccionado y trazable · deps auditadas en CI |
| **WP-V86** `P0` | **PRIMERA release pública aceptada** — absorbe al viejo V52: semver lo decide el custodio (no hay 0.1/0.2 que salvar); artefacto firmado/hash; guía apunta al artefacto. **Decisión ③ (custodio 2026-07-31): corte con el histórico** — scope `alephscriptorium/aleph-scriptorium` muerto; identidad pública muy Scriptorium, ligada al scope nuevo (github + registry npm nuevos · v-sdk.escrivivir.co); contadores reiniciados; la Release v0.1.0 antigua se desconecta y depreca; canal = GitHub Release; Marketplace **DEFERRED** (spike market propio → O96) | DoD R11-V completa · sha asset = sha local · anomalía del v0.1.0 extinguida |
| **WP-V53** `P2` | **Portar workflows a Forgejo** (W-2): prueba de facto de portabilidad | los dos workflows verdes en la forja sin reescribir |
| **WP-V11** `P2` | **Atlas y punteros**: gitlink `codebase/v-sdk` + notas | R6-V PASS ya ✅ · falta GO DA-S11 |

## LANE I · LA CAMPANA — la mesa como caso de uso — **P2**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V54** `P2` | **Hospedar la campana**: campanilla FS → `parte-kit` sobre mesh vía `operator-bridge` | un PING llega por mesh · la mesa se notifica con piezas que censa |
| **WP-V55** `P2` | **Sincronía como superficie** desde el IDE | sala legible sin salir del editor · escritura sigue en estrella |

## LANE J · JUEGO Y 3D — **P2 (horizonte)**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V56** `P2` | **Catálogo de juegos de G** en el IDE: existe ≠ tiene datos | sin inventar disponibilidad |
| **WP-V57** `P2` | **Ventanas 3D** (`solar/force/player-3d`): la decisión superficie-o-enlace se toma **por pieza contra V20** — habrá de ambas | cada pieza con fila y motivo · si enlace, dentro del cerco |
| **WP-V58** `P2` | **`editor-ui`/`player-ui`**: frontera declarada por pieza (webview, enlace o vecina — según contrato Z/G, no por preferencia) | frontera declarada · cero duplicación de función |

## LANE K · L1 Y FRONTERA CON O — **P2 (horizonte)**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V59** `P2` | `ssb-system` en lectura: L1 visible, jamás escritura desde la sala | cero escritura al pub · L1 marcado |
| **WP-V60** `P2` | `blobstore-client` en lectura con frontera O | CID visible · el IDE no posee el blob |
| **WP-V61** `P2` | `acta-kit`/`parte-kit` como superficie | acta legible entera · cristalización nunca desde aquí |

## LANE L · PRODUCTO — instalable por un desconocido — **P0/P1**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V68** `P0` ✅ | **Arnés Extension Host**: `tests/exthost/` (lanzador + suite in-host + acta JSON anti-verde-por-construcción) sobre VS Code 1.131.0 real; **modo `.vsix` probado** — corre desde el artefacto instalado, no del fuente. Job `exthost` en ci.yml (run verde en CI = ⏳ honesto hasta el primer push — se verifica en el gate R7-V). Guardas probadas de facto (caso malo → ROJO exit 1). **Hallazgo ORO → V25/V72**: **31/99 comandos contribuidos SIN registrar + 5 registrados sin fila en el manifiesto** (lista en `plan/REPORTES/WP-V68-arnes-exthost.md`) · host transitoriamente unresponsive en activación (→V73/V88). Aceptado 2026-07-31 (rama `wp/v68-arnes-exthost`; diff package.json = 2 líneas de scripts, 0 devDeps nuevas — `@vscode/test-electron` ya estaba) | activación/comandos/vistas probados en VS Code real ✓ (local ambos modos; CI ⏳ primer push) · el ⏳ estructural de la guía muere ✓ |
| **WP-V84** `P0` | **Matriz limpia de plataforma**: VS Code mínimo real, Windows/Linux, `.vsix` sin repo ni node_modules | instala y activa en máquina limpia · `engines.vscode` verificado, no supuesto (absorbe la mitad viva del viejo V74) |
| **WP-V82** `P0` | **Workspace Trust + secretos + permisos**: workspace no confiable no ejecuta procesos; secretos en SecretStorage/env | untrusted = cero spawn · cero secretos en settings/logs |
| **WP-V62** `P1` | **Ciclo de vida**: activación por evento real, `deactivate`/`dispose` completos | cerrar la ventana no deja procesos ni watchers |
| **WP-V63** `P1` | **Estado declarado y regenerable** | inventario de estado · borrarlo no rompe · nada contradice al env |
| **WP-V64** `P1` | **Superficie de error**: qué pasó y qué hacer, siempre | cero excepciones mudas · cero éxito parcial fingido |
| **WP-V65** `P1` | **Resiliencia**: reconexión con backoff, sin tormentas ni zombis | sobrevive caída y vuelta · el corte se ve mientras dura |
| **WP-V90** **`P0`** ✅ | **ACEPTADO 2026-08-01** (merge `a0ca305`, 1 contrarrevisión adversarial con 4 bloqueantes + ronda de corrección + verificación del orquestador sobre informe **real** de jest). **El método es lo mejor del WP: el worker se negó a hacer lo que le pedí.** Yo pedía diez corridas idénticas; respondió que **diez corridas iguales no prueban nada** porque en serie salen iguales sin arreglar nada, y montó **tres brazos** — A (sin arreglar, cargada) **6 de 10 discrepantes** · B (arreglado, misma carga) **10/10 por nombre** · C (arreglado, tranquila) **10/10**. Con control, no sólo con tratamiento. Y **los tres flapeadores del brazo A son exactamente tres de los censados**: el censo no es una lista plausible, es la lista de los culpables con confesión. El brazo C lo reprodujo el contrarrevisor **con contención real**. **Corrigió dos cosas del brief que resultaron falsas al medirlas**: los relojes falsos **no dejan la duración en 0** — la dejan valiendo *exactamente lo avanzado*, o sea que vuelven la aserción **tautología sobre el guion del propio test** (por eso reloj controlado en **un** sitio y retirada en los otros siete); y cae la **cota superior**, nunca la inferior. **Honestidad que consta**: declara que **sólo 3 de las 12** se vieron caer y que las otras 9 se retiran **por argumento**, en columna propia. El contrarrevisor verificó los nueve: las nueve dependen de la máquina, ninguna determinista se retiró sin motivo. **Los cuatro bloqueantes**, el primero con mucha ironía: **(B1)** el gate comparaba **conjuntos sin multiplicidad**, así que un rojo nuevo con nombre ya declarado pasaba entero — **el cardinal subió de 5 a 6 y el gate que vino a abolir el cardinal no se enteró**; en ese vector el instrumento viejo era **estrictamente más fuerte**. **(B2)** `--repetir` declaraba «las N corridas dieron el MISMO conjunto» con EXIT=0 **cuando jest no había ejecutado un solo test**. **(B3)** leía el JSON **sin prueba de frescura** — cerrado por el lado fuerte: `--gate` **corre jest él mismo** a un fichero con nonce fuera del árbol, con lo que la clase entera desaparece porque el gate controla su propia medida (y retiró su recomendación de encadenar con `;`, que **era** el vector). **(B4)** decía «cuatro nombres que prometen de más»; eran **nueve** — un cardinal mal contado en el reporte de un WP cuyo pecado original es un cardinal mal contado en un censo sobre cardinales. Lo llamativo: **su propio gate lo habría cazado si se lo hubiera aplicado a su reporte**. **Verificación del orquestador** sobre informe real: control → `IDENTICO`, exit 0; duplicado inyectado → exit 1 con `[sobran 1 de 2]`; `--repetir` sin ejecución → exit 2; JSON rancio → exit 2 citando la edad; `--runInBand` en args extra → exit 2. Árbol del worker limpio todo el tiempo. **Cambio de mayor radio, señalado por él para que se viera antes de fusionar**: borró `tests/performance/serviceStartup.test.ts` **entero** ✎ *(esta ruta no resuelve **a propósito**: es el acta de su borrado, y fue este mismo WP quien lo borró. El barrido de citas la marca `RANCIA` por construcción — **toda fila que registre una poda citará una ruta muerta**, y esa clase no es un defecto sino el límite del instrumento; anotada, no corregida)* — importaba **cero líneas de producto**, así que sin los relojes sus tests sólo aseveraban sobre datos que ellos mismos construían: cinco verdes perpetuos en un fichero llamado «Performance Tests». Con él se fue `measurePerformance` de `setup.ts`, sin un solo consumidor. **El árbol final no contiene ni una lectura de reloj de pared** fuera del reloj falso, y el baseline no se movió. Cobertura real **24,91 %**, no el 12,6 % que circulaba. Sale a **V91** (el instrumento sin gate propio) y **V92** (citas rancias); reasigna **V48** | las aserciones de reloj de pared sustituidas o marcadas ✓ · 10 corridas con el mismo conjunto **por nombre** ✓ (medido en dos brazos) · el gate compara **nombres, no cardinales** ✓ |
| **WP-V89** `P1` | **Endurecimiento del censo de puntos de render** (nace de la 2.ª contrarrevisión de V66, 2026-07-31 — **partido a propósito**). El censo derivado del AST cazó los tres bypass enunciados, pero es **sintáctico donde debe ser semántico** y cae con variantes de una línea: **DD1** la regla de sumideros reconoce una *gramática* (`EqualsToken` + `.html` como identificador, clave `html` sin comillas) y no una *operación* → evaden clave entrecomillada, clave calculada, `Object.defineProperty`, `+=` · **DD2** `pathToCensus` resuelve por **nombre simple global** y sólo inspecciona el camino más corto: colisionar un nombre deja `chain.slice(0,-1)` vacío y **cero** intermediarios verificados; una llamada señuelo a cualquier render censado cortocircuita el BFS · **DD3** `DOC_SIGNAL`/`FRAG_SIGNAL` son textuales sobre literales propios: `String.fromCharCode(60)` deja la función en `kind:'none'`. **Modelo de amenaza a fijar**: el censo defiende contra **regresión en `src/`**, no contra un contribuyente hostil deliberado (quien edita `src/` edita el test) — la frontera de seguridad real es la guarda de ejecución. Insumo: apartado de aprendizaje que escribe el worker de V66 | la unidad verificada es la **operación** de asignación, no su sintaxis · resolución por identidad de módulo, no por nombre simple · el señuelo no cortocircuita · modelo de amenaza escrito en el contrato |
| **WP-V66** `P1` ✅ | **Seguridad de webviews · ACEPTADO 2026-08-01** (merge `299e197`, 5 rondas · 5 contrarrevisiones). Verificado por el orquestador: webviews **128/128**, conjunto completo **245** con los 5 rojos históricos y cero regresión. **Cierra la guarda de ejecución**, que es la frontera real contra entrada externa: tokenizador con **fail-closed** (lo no analizable se rechaza, y aun así **209/210** del árbol pasan) · **normalización de URL convergente** — una puerta, dos clasificadores — tras **1026 casos dirigidos y 15.350 mutaciones** contra el parser WHATWG sin una divergencia insegura · atributos clasificados **por lo que la URL hace** (`resource` vs `navigation`): el falso positivo de `<a href>` pasó de **202/209 ficheros a 0** sin relajar `ping` ni `form action`, que son exfiltración · `<image>` **arreglado en vez de anotado** (rechazaba de menos) · y la capa que no depende del análisis: **si el contenido viene de disco no hay scripts**, diga lo que diga la config. Esa capa —decisión propia del worker— es la razón de que cuatro rondas de hallazgos en el analizador dieran **cero incidentes de ejecución**. Lección que deja escrita: *la prisa por cerrar un hallazgo produce el siguiente, y el reporte tiende a describir la intención en vez del código*. El censo sale a **V89** | ~~cero `unsafe-inline`~~ ✓ · CSP verificada por test ✓ · los bypass en rojo ✓ |
| ~~**WP-V66**~~ (histórico de la devolución) | **Seguridad de webviews**: CSP, nonce, `localResourceRoots`. **La 2.ª contrarrevisión halló 4 bypass**; el orquestador **parte el WP**: V66 cierra solo las **vulnerabilidades reales** — **DD4** `attrOf` (`security.ts:186`) sólo parsea valores **entrecomillados**, así que `src=https://evil.example/x.js` sin comillas devuelve `undefined` y el bucle de `URL_BEARING` hace `continue` (ídem `onXXX=` y `style=`) · **DD5** `stripHtmlComments` (`:177`) **desincroniza con el tokenizador HTML** (`<!-->` abrupt-closing y `<!--` dentro de valor de atributo) → marcado que el navegador ejecuta desaparece del análisis. **Ambas explotables sin tocar `src/`**, por el HTML de disco que `getDriverUIConfig()` sirve con `enableScripts:true`. Lo estructural (censo) sale a **V89**. Instrucción de fondo: dejar de analizar HTML hostil con regex — **si no se puede analizar con confianza, se rechaza**. Obra sustancial **viva en rama `wp/v66-csp`** (tip `9f0a5d7`, worktree conservado) — **no mergeada**: la contrarrevisión adversarial halló 3 bypass no cazados (censo por fichero y no por punto de render · **script externo CON nonce, explotable** · `hasCspMeta` burlable con comentario HTML) + inyección de directivas en el helper. Defectos numerados y lo que SÍ resiste: **`plan/DEVOLUCION-V66-csp.md`**. **Corrección despachada 2026-07-31 al deshielo** (worker fresco, misma rama y worktree, commits nuevos; al cerrar, **nueva contrarrevisión adversarial obligatoria** que reintente los 8 vectores) | cero `unsafe-inline` · CSP verificada por test · los 3 bypass en rojo |
| **WP-V67** `P1` | **Tema y accesibilidad** | legible en claro/oscuro · navegación por teclado |
| **WP-V71** `P1` ✅ | **OutputChannel + log estructurado · ACEPTADO 2026-08-01** (merge `56b5265`, 3 rondas · 2 contrarrevisiones). Verificado por el orquestador: logging **135/135**, conjunto **252** con los 5 rojos históricos, y la **probe de paridad ejecutada por mí: 0 desvíos de nivel**. **La equivalencia 1:1 dejó de ser inspección y pasó a ser medida** — comparador que cotea base y árbol llamada a llamada, y que tras la devolución **viaja en el entregable** (`scripts/probes/`), porque una medida que nadie puede repetir es una cita. **La redacción se arregló de raíz a la segunda**: desaparece la lista aparte para claves y queda **un solo vocabulario** del que salen las dos mitades, con el invariante fijado **en pareja** por término — la frontera que se había roto dos veces (`?auth=` sí y `--auth` no; luego `apikey` en claro junto a `apiKey` tapado **en la misma línea**). Los **límites van fijados por test**: cerrarlos pone rojo. El gate es AST, no regex (descubrió que la regla estándar sola caza 6 de 9 alias). **Dos gestos que constan**: probó restringir `globalThis` entero, vio que rompía un uso legítimo y **lo deshizo** declarando que su «cero usos» venía de un grep truncado; y declaró que sus 135 tests nuevos **agravan el flapeo de reloj del mundo** (→ V90), efecto real de su cambio dicho sin que nadie preguntara | cero `console.log` sueltos ✓ · depurable en máquina ajena ✓ · cero cambio observable **demostrado** ✓ |
| ~~**WP-V71**~~ (histórico) | **OutputChannel + log estructurado**. **Ola 3, despachado 2026-07-31** (`wp/v71-log-estructurado`, worktree `wt/v-v71`) — habilitado por V80 (troceo del bootstrap: ya no colisiona con V25/V62/V64); **prohibido tocar `package.json`** (lo tiene V23) · **contrarrevisión obligatoria** §4.5 cero-cambio-observable | depurable en máquina ajena · cero `console.log` sueltos |
| **WP-V72** `P1` | **Menús/keybindings/`when`** coherentes con lo real | cero menús a comandos inexistentes |
| **WP-V85** `P1` | **Privacidad y datos**: inventario de datos/red/logs; opt-in explícito | cero telemetría accidental · inventario publicado |
| **WP-V87** `P1` | **Actualización/rollback**: update fallido no pierde env/estado | rollback probado · uninstall coordina V75 |
| **WP-V88** `P1` | **Presupuestos de recursos**: activación, memoria, refrescos medidos en CI | presupuesto declarado y vigilado · refrescos sin bucle |
| **WP-V69** `P2` | Multi-root y workspace sin carpeta | abre sin carpeta · multi-root declarado |
| **WP-V70** `P2` | **Primer arranque** comprensible sin README | camino claro al env |
| **WP-V73** `P2` | **Coste**: 99 comandos, árboles, activación — medir antes de crecer | presupuesto de activación declarado |
| **WP-V74** `P2` | **Plataforma soportada** (lo que queda del viejo V74 tras V84): política de versiones VS Code hacia delante | política escrita · cero deuda de «migrar usuarios» inexistentes |
| **WP-V75** `P2` | **Desinstalación limpia** | ni procesos, ni ficheros fuera de ámbito, ni ajustes huérfanos |
| **WP-V76** `P1` | **CA del centro vacío** verificable (dep V68) | cero pestañas abiertas por la extensión · el test falla si un WP futuro rompe la doctrina |

## LANE M · GOBIERNO Y FUNDACIÓN — **P0**

| WP | brief | CA tentativo |
| -- | ----- | ------------ |
| **WP-V81** `P0` ✅ | **Fundación del plan**: `plan/VISION.md` + `plan/PRACTICAS.md` — contrato local único del carril (invariantes I-1..I-5 literales en PRACTICAS §1; ejes CA §3; riesgo §4; evidencia §5). **El backlog apunta a ese par y no lo duplica.** Aceptado por el orquestador 2026-07-31 (rama `wp/v81-fundacion`) | contrato local único ✓ · invariantes fuera del backlog ✓ (worker no tocó BACKLOG; verificado) |
| **WP-V77** `P0` ✅ | **Grafo de deps y contención**: `plan/GOBIERNO-EJECUCION-F2.md` §1–§3 — **aceptado por el orquestador (2026-07-31, GO ola 0)**; correcciones de forma aplicadas (V52→V86 · puerta=rooms) | lotes paralelos con alcance disjunto verificado |
| **WP-V78** `P0` ✅ | **Gates por lane + definición de terminada**: íd. §4–§5 — **aceptado por el orquestador (2026-07-31, GO ola 0)** | cada lane con gate y evidencia · «terminada» = CAs, no opinión |
| **WP-V79** `P1` ✅ | **Plantilla de brief**. **ACEPTADA 2026-08-02 por el orquestador, midiendola y no leyendola** — y con **cuatro defectos corregidos en el acto**, tres de ellos trampas caducadas que nada delataba: **(1)** la ruta de worktree que daba, `C:/S_LAB/.worktrees/v/…`, apunta a un directorio que **existe y esta VACIO** —peor que una ruta inexistente: parece plausible y no falla al mirarla—; la convencion viva es `C:/S_LAB/wt/<mundo>-<wp>`. **(2)** declaraba «npx esta ROTO en esta maquina» y `npx --version` responde **10.9.4**; la regla que si rige es 4-ter (npx no declarado, prohibido). **(3)** citaba «los 5 tests rojos preexistentes, su arreglo es WP-V48»: **V48 esta aceptado** y el conjunto declarado tiene hoy **UNA entrada, un OMITE honesto de V28, cero fallos**. **(4)** no traia la **calibracion de identidad-raiz**, que el skill de orquestacion exige literalmente en cada despacho («si falta cualquiera, no hay despacho ni boot») — ahora es la primera seccion del bloque. **La regla que sale de aqui y que ya esta en el encabezado de la plantilla**: *un censo de trampas sin fechas envejece en silencio*. Cada trampa lleva ahora su fecha de medicion, y las que no se re-midieron **lo dicen**. Estrenada en el mismo bloque: los seis briefs de B9 se escribieron verificando cada fichero contra el arbol antes de citarlo | un worker nuevo no repite las trampas censadas ✓ · **y las trampas no mienten** ✓ (4 corregidas con orden y salida) |

---

## Conteo

| prioridad | WPs |
| --------- | --- |
| **P0** | **13** — V18 · V20 · V21 · V22 · V26 · V28 · V68 · V77 · V78 · **V81 · V82 · V84 · V86** |
| **P1** | **37** — V19 · V23 · V24 · V25 · V27 · V29 · V30 · V31 · V32 · V34 · V35 · V36 · V37 · V38 · V39 · V42 · V43 · V44 · V45 · V49 · V50 · V51 · V62 · V63 · V64 · V65 · V66 · V67 · V71 · V72 · V76 · V79 · V80 · **V83 · V85 · V87 · V88** |
| **P2** | **21** — V11 · V33 · V40 · V41 · V46 · V47 · V48 · V53 · V54 · V55 · V56 · V57 · V58 · V59 · V60 · V61 · V69 · V70 · V73 · V74 · V75 |
| **total F2-unificada** | **71** en **13 lanes** *(V52 absorbido por V86)* |

`BLOQUEA:` — **V18** (holón-7) · **V20** (modelo de integración) ·
**V86** (nada es producto sin primera release aceptada).

Compromisos externos: **T9 → V44** (evidencia a HUB-084) · V26 consume
schema de O+Z · V34 consume U234 · V18 consume Z-D1.

---

## Histórico (cerrado · `[cita inerte]`)

Olas 0–F (V01–V17) ✅ hasta R6-V · detalle en `plan/REPORTES/` ·
decisiones en `plan/DECISIONES.md`.

---

**Nada despachado.** Todo espera aprobación o descarte del custodio.

— **V** · Aleph-0 (ℵ₀) · *edición F2-unificada por el Anfitrión*
